# LIPLAB UI 구현 지도 — Figma ↔ 코드 연결 명세

> 이 문서는 Claude Code가 Figma 리디자인을 코드에 적용할 때 **먼저 읽고 따르는 기준 문서**이다.
> Figma MCP는 "화면이 어떻게 생겼는지"를 알려주고, 이 문서는 "그 화면이 어느 기능·라우트·상태인지, 어디서 어디로 이어지는지"를 알려준다.

- 기준 브랜치: `feat/sublexical-feedback`
- Figma fileKey: `yF6TQfdpIqZ9jUQxDbFIEa` / 페이지: `03. App`(랜딩은 `02. Landing`)
- 노드 링크 형식: `https://www.figma.com/design/yF6TQfdpIqZ9jUQxDbFIEa/LIPLAB?node-id=58-11` (노드 ID의 `:`를 `-`로 바꾼다)
- 이 문서를 쓴 뒤 Figma에서 바뀐 내용은 `LIPLAB_UI_CHANGELOG.md`(디자인 쪽 변경 내역, 9/26 받음)에 있고, 충돌하면 그 문서가 먼저다.
  9/26 적용 결과와 Figma·변경 내역이 서로 다른 곳은 §6-8.

---

## 0. 작업 규칙 (반드시 지킬 것)

1. **기준의 분담.** 모양(색·간격·크기·문구)은 Figma가 기준이고, 화면과 기능의 연결은 이 문서가 기준이다. `LIPLAB_REDESIGN_SPEC.md`나 코드 주석과 충돌하면 이 문서를 따른다.
2. **새 라우트·페이지·컴포넌트를 임의로 만들지 않는다.** 이 문서에 없는 화면이 필요해 보이면 작업을 멈추고 질문한다.
3. **Figma 프레임 하나가 페이지 하나가 아니다.** 작업 전에 §2 "한 화면의 상태 목록"을 확인한다.
4. **문구는 Figma 텍스트를 그대로 쓴다.** 다듬거나, 설명을 덧붙이거나, 이모지를 넣지 않는다.
5. **Figma에 없는 요소는 만들지 않고, Figma에서 빠진 요소는 코드에서도 지운다.** (예: 설명 문구, 들어보기 버튼, 다시 보기 버튼, 로그아웃 버튼)
6. **색·간격·반경은 디자인 토큰만 쓴다.** 임의 hex·px 값을 새로 만들지 않는다.
7. **로직은 건드리지 않는다.** API 호출·채점·상태 관리는 그대로 두고 UI 계층만 교체한다. 로직 변경이 필요하면 멈추고 질문한다.
8. **한 번에 한 작업 단위(§8)만 진행한다.** 끝나면 Figma 스크린샷과 구현 화면을 비교해 다른 점을 목록으로 보고한다.
9. **구현 대상이 아닌 것:** `_시안 / 경로 노드`(354:84), 페이지 `99. Archive`, 가이드 모달 속 화면 축소본(실제 컴포넌트가 아니라 이미지로 내보내 쓴다).

---

## 1. Figma 섹션 구성

| 섹션 | 내용 | 종류 |
|---|---|---|
| 00. 로그인 · 회원가입 | 로그인, 회원가입 | 비로그인 화면 |
| 01. 온보딩 | 자가진단 안내, 자가진단 문항, 결과 | 온보딩 흐름 (배치검사) |
| 02. 학습 — 커리큘럼 | 학습 탭의 상태 5개 | `/learn/path` 상태 |
| 03. 독화 레슨 | 문제, 정답, 오답, 완료 | 레슨 집중 모드 상태 |
| 04. 발화 레슨 | 발화 전, 녹음 중, 결과, 상세 모달, 결과(아쉬움) | 레슨 집중 모드 상태 |
| 05. 기타 탭 | 연습, 과제, 복습, 분석, 프로필 | 탭 페이지 |
| 06. 과제 · 복습 상세 | 배지 모달, 복습 선택 모드 | 모달 / 상태 |
| 07. 분석 상세 (모달) | 활동 캘린더, 회차 히스토리, 전체 통계, 회차 상세 | 모달 |
| 08. 프로필 상세 (모달) | 계정 설정, 학습 초기화 | 모달 |
| 09. 사용법 가이드 (모달) | 가이드 모달의 탭 11개 | 모달 하나의 탭 상태 |
| 10. 로딩 | 트랙별 로딩, 기본 로딩 | 로딩 상태 |
| 11. 연습 기능 | 연습 탭에서 들어가는 기능 5개 | 탭 하위 페이지 |
| 12. 모바일 | 위 화면들의 모바일 버전 | **같은 라우트의 반응형** |

`02. Landing` 페이지의 `Landing — Desktop`(9:12)은 비로그인 첫 화면이다(§4-00, 9/25 추가).

---

## 2. 프레임 ≠ 페이지: 한 화면의 상태 목록

아래 프레임들은 **같은 라우트·같은 컴포넌트의 상태**다. 프레임마다 페이지를 만들지 않는다.

| 라우트 / 컴포넌트 | 상태 | Figma 노드 |
|---|---|---|
| `/learn/path` · `CurriculumPath` | 진행 중인 단계 | 58:11 |
| | 다음 단계 잠김 + 건너뛰기 말풍선 | 78:8 |
| | 건너뛰기 확인 (모달) | 79:5 |
| | 건너뛰기 후 해금 | 80:6 |
| | 발화 트랙 선택 시 (분홍) | 171:38 |
| 독화 레슨 공통 템플릿 | 문제 (보기 미선택, 확인 비활성) | 91:12 |
| | 정답 피드백 | 94:98 |
| | 오답 피드백 | 94:140 |
| | 레슨 완료 | 93:12 |
| `/learn/speaking` · `SpeakingPractice` | 말하기 전 | 175:21 |
| | 녹음 중 | 175:49 |
| | 결과 (통과) | 176:21 |
| | 자세히 보기 (모달) | 182:21 |
| | 결과 (아쉬움) | 186:21 |
| `/review` · `ReviewTab` | 기본 | 100:15 |
| | 지우기 → 선택 모드 | 318:33 |
| 사용법 가이드 모달 | 탭 11개 (왼쪽 탭 목록의 선택 상태만 다름) | 338:57 ~ 342:348 |
| `/learn/placement` · `Placement` | 문항 (정답 비공개) | 385:82 |
| | 결과 (온보딩 모드) | 85:9 |
| `Login` | 로그인 / 회원가입 전환 | 227:35 / 227:74 |
| 모든 화면 | 모바일 버전 (§4-12) | `M / …` 프레임 |

---

## 3. 전역 규칙

### 3.1 색 = 트랙
- 독화 `#7D53DE`(primary) · 발화 `#EC4899`. 단계별로 색을 바꾸지 않는다.
- 발화 트랙 화면은 사이드바 활성 탭·배너·버튼·진행률까지 분홍으로 바뀐다.
- 트랙과 무관한 고정 색: 북마크 복습 카드 `#2563EB`(199:29), 스탯 칩(불꽃·별·레벨)은 각자 고정 색.
  질문 옆 북마크 버튼(328:8)은 켜지면 보라(`#7D53DE` 테두리·`#EFE9FC` 바탕)다(9/25 Figma 대조로 보탬).

### 3.2 점수 색
- 음소별 점수: 70 이상 초록, 45~69 주황, 45 미만 빨강.
- 정답률: 85 이상 초록, 65~84 주황, 65 미만 빨강.

### 3.3 AppShell
- 데스크톱: 왼쪽 사이드바 6탭(학습·연습·과제·복습·분석·프로필) + 오른쪽 패널(오늘의 과제 · 복습 카드).
- 모바일(`lg` 미만): 상단 바(로고 + 불꽃 · 별 · 프로필 사진) + 하단 탭 5개(프로필 제외). 프로필은 상단 사진으로 들어간다. 상단 바는 흰 배경 없이 페이지 배경과 이어진다.
- 오른쪽 패널 복습 카드: `오답 N개 │ 북마크 N개`(가운데 정렬, 숫자만 크게, 오답 숫자 보라·북마크 숫자 파랑) + 아래 `복습하기` 버튼 → `/review`.

### 3.4 레슨 집중 모드
- 독화·발화 레슨, 자가진단(배치검사)은 AppShell 없이 전체 화면으로 띄운다.
- 상단: X(나가기) + 진행률 바 + `n / 전체`. 하단: 고정 액션 바(버튼은 오른쪽).
- 질문 제목 오른쪽 끝에 `Bookmark button` 컴포넌트(328:8). 기본은 테두리만, 누르면 채워진 상태.
- 입모양 영상은 무한 반복 재생이므로 "다시 보기" 버튼이 없다. "들어보기"(소리 재생) 버튼도 없다(청각장애 대상).

### 3.5 모달
- 배경 딤 + 가운데 흰 카드 + 오른쪽 위 원형 X. 모달 밖을 눌러도 닫힌다.
- 예외: **배지 상세 모달**은 카드 없이 배경 블러 위에 배지·문구·닫기 버튼만 띄운다.

### 3.6 워터마크 카드
- 숫자 카드(분석 탭 스탯, 레슨 완료 스탯, 복습·과제 히어로 카드)는 아이콘을 오른쪽 위에 크게 걸쳐 잘리게 둔다.
- 규칙: 회전 +20°(반시계), 투명도 20% 내외, 카드 밖으로 넘치는 부분은 잘림(`overflow: hidden`).

### 3.7 학습 경로의 DOKA 노드
- 경로 노드는 원형 버튼이 아니라 마스코트 DOKA다.
  - 완료: 트랙 색 DOKA, 투명도 55%, 오른쪽 아래 체크 배지
  - 현재: 트랙 색 DOKA, 1.15배 크기, 링
  - 잠김: 회색으로 눈 감은(잠든) DOKA
  - 건너뛰기 가능: 연한 트랙 색, 눈 뜬 DOKA
- 경로 옆에 따로 서 있는 큰 DOKA는 없다.

---

## 4. 화면별 명세

### 4-00. 로그인 · 회원가입
| Figma | 대상 | 비고 |
|---|---|---|
| 9:12 랜딩(Desktop) | `pages/Landing.jsx` | `App.jsx` AuthGate가 비로그인 `/`에서 렌더. "시작하기" → `/signup`, "계정이 이미 있습니다" → `/login`. Figma는 데스크톱뿐이라 lg 미만은 세로로 쌓고 그림은 zoom으로 줄인다. 제목 글꼴은 Figma가 Josefin Sans(한글은 대체 글꼴)라 Noto Sans KR로 쓴다(64px에서 폭 거의 같음) |
| 227:35 로그인 | `pages/Login.jsx` | `App.jsx` AuthGate가 비로그인 `/` 밖의 경로에서 `<Login />` 렌더(`/login`·`/signup`은 AuthGate만 읽는 주소, `/signup`이면 회원가입 모드로 연다). 이메일·비밀번호, 로그인, 둘러보기(데모 → `/api/auth/demo`) |
| 227:74 회원가입 | `Login.jsx`의 회원가입 모드 | 이메일·사용자명(2~50자)·비밀번호(6자 이상), "이용약관 및 개인정보 처리방침에 동의해요" 체크. 코드는 법적 요건으로 만 14세 확인 줄을 한 줄 더 둔다 |

### 4-01. 온보딩 · 자가진단 (배치검사)
- **자가진단 = 기존 배치검사(`Placement.jsx`)를 그대로 쓴다.** 적응형 8문항, 문항 선택·채점·시작 단계 추천 로직은 바꾸지 않고 UI만 교체한다.
- `Placement.jsx`는 사전·사후 평가(통제 향상도)에도 쓰인다. **평가 모드의 결과 리포트는 지금 그대로 유지**하고, 아래 Figma 결과 화면은 온보딩(배치) 모드에만 적용한다.

| Figma | 대상 | 비고 |
|---|---|---|
| 84:7 자가진단 안내 | `/onboarding` · `Onboarding.jsx` | 기존 트랙 선택 UI(👁 등)를 이 화면으로 교체. "시작하기" → `/learn/placement`, "나중에 할게요" → `/learn/path` |
| 385:82 자가진단 문항 | `/learn/placement` · `Placement.jsx` 문항 상태 | 독화 레슨 템플릿과 같은 틀. 북마크 없음, 정답 즉시 공개 없음. 하단 바 왼쪽 "정답은 끝나면 결과로 알려드려요", 오른쪽 "다음" |
| 85:9 결과 | `Placement.jsx` 결과 상태 (배치 모드) | "학습 준비가 다 되었어요!" + "학습하러 가기"(기존 추천 시작 단계로 이동하는 로직 그대로) + "다시 진단하기" |
- 프로필의 "자가진단 다시 하기"는 확인 모달 없이 바로 `/learn/placement`로 간다.

### 4-02. 학습 탭 — `/learn/path` · `CurriculumPath`
- §2의 상태 5개. 상단 `[독화] [발화]` 트랙 전환, 단계 배너(가이드 버튼), DOKA 경로(§3.7), 좌우 단계 이동 화살표, 현재 노드 옆 레슨 카드(진행률 + 이어서 학습하기).
- 잠긴 단계: 건너뛰기 말풍선 → 확인 모달 → 해금. 기존 `StageGate` 로직을 그대로 쓰고 UI만 바꾼다.

### 4-03. 독화 레슨 — 공통 템플릿
- 4지선다 문항을 내는 단계 화면(`VisemeLiteracy`, `WordStage` 등)에 공통 적용한다. **적용할 파일 목록을 먼저 조사해 보고하고, 확인받은 뒤 적용한다.**
- 흐름: 문제(91:12) → 보기 선택 → 확인 → 정답(94:98) 또는 오답(94:140) 하단 바 → 계속하기 → … → 완료(93:12).
- 보기는 클릭 또는 숫자 키 1~4.
- 완료 화면: DOKA + "레슨 완료!" + 워터마크 스탯 3칸(정답률 · 획득 XP · 걸린 시간) + "다음 레슨으로" / "커리큘럼으로 돌아가기". **현재 코드에 없는 상태**이므로 새로 만든다(페이지가 아니라 레슨 컴포넌트의 마지막 상태).

### 4-04. 발화 레슨 — `/learn/speaking` · `SpeakingPractice`
- `/review/speaking/session`도 같은 컴포넌트이므로 같은 UI를 쓴다.
- 발화 전(175:21): 말할 단어 카드(가운데 정렬, 분홍) + 3D 아바타 + 하단 가운데 마이크.
- 녹음 중(175:49): 마이크 → 정지 아이콘, 펄스 링.
- 결과(176:21 / 186:21): 발음 정확도 % + 음소 칩(§3.2) + "자세히 보기". 하단 바: 통과는 초록 "잘했어요!", 미통과는 빨강 "조금 더 연습해요". 버튼: 다시 말하기 / 계속하기(통과) · 넘어가기 / 다시 말하기(미통과).
- 자세히 보기(182:21): 모달. 이렇게 들렸어요 · 음소별 정확도 · 소리+입모양 융합 점수 · "DOKA의 한마디" 코칭.

### 4-05. 탭 페이지
| Figma | 라우트 · 파일 | 비고 |
|---|---|---|
| 96:14 연습 | `/practice/hub` · `PracticeHub` | 카드 4개(자유 발화·상황별 시나리오·수어 보기·엔드리스 학습) + 공사 중 칸(코드는 '입모양 교실'). 다자 대화 카드는 9/26 변경 내역 §2로 뺐다. §6-2 |
| 137:17 과제 | `/tasks` · `TasksPage` | 오늘의 과제(n/n + XP), 이번 주 도전(워터마크 DOKA), 배지 12종. 배지 클릭 → 배지 모달(313:33) |
| 100:15 복습 | `/review` · `ReviewTab` | 오답 복습 → `/review/mistakes`, 북마크 복습 → `/review/saved`. 목록 필터(전체·오답·북마크), 지우기 → 선택 모드(318:33) |
| 104:15 분석 | `/analysis` · `AnalysisTab` | 워터마크 스탯 3칸, 학습시간 추이(막대), 정확도 추이(선+수치), 버튼 3개 → 모달(§4-07) |
| 107:16 프로필 | `/profile` · `ProfilePage` | 메뉴 4개: 사용법 가이드(모달) · 자가진단 다시 하기(`/learn/placement`) · 계정 설정(모달) · 학습 초기화(모달). §6-3 |
- 탭 제목 아래 설명 문구는 없다.

### 4-06. 과제 · 복습 상세
| Figma | 대상 | 비고 |
|---|---|---|
| 313:33 배지 상세 | `TasksPage` 안의 모달 | 배지 크게 + 은은한 흰 원 + "전체 사용자 중 N%가 획득했어요" + 닫기. 희귀도 값이 없으면 그 줄을 숨긴다. §7-3 |
| 318:33 선택 모드 | `ReviewTab` 상태 | 체크박스 + 전체 선택 · 취소 + 하단 "선택한 N개 지우기"(빨강) |

### 4-07. 분석 상세 — 모두 `AnalysisTab`에서 여는 모달
| Figma | 모달 | 비고 |
|---|---|---|
| 206:305 | 활동 캘린더 | 20주 잔디, 5단계 보라 |
| 209:22 | 회차 히스토리 | 날짜(오늘·어제·N일 전) · 트랙 · 레슨 · 정답률 막대 · 문제 수 · 정답률 · `>`. §7-1 |
| 210:23 | 전체 통계 | 총 학습 회차 · 푼 문제 · 획득 배지 + 트랙별 진도 |
| 212:24 | 회차 상세 (발화) | 히스토리 행 클릭 시. 소리/입모양/융합/불확실성 + 문제별(음소 칩 · 들림 · 점수) + DOKA의 한마디. §7-2 |
- 기존 `/analysis/activity`, `/analysis/history` 등 상세 라우트는 이번 작업에서 삭제하지 않는다.

### 4-08. 프로필 상세 — `ProfilePage`에서 여는 모달
| Figma | 모달 | 비고 |
|---|---|---|
| 220:27 | 계정 설정 | 사진 변경, 이름·이메일·비밀번호 수정, **로그아웃**, 계정 삭제 |
| 222:28 | 학습 초기화 | 사라지는 기록 목록 + "초기화" 입력해야 버튼 활성 → `/api/analysis/reset` |

### 4-09. 사용법 가이드 — `ProfilePage`에서 여는 모달 하나
- 왼쪽 세로 탭 목록 + 오른쪽 내용. 탭: 화면 둘러보기 · 학습 탭 · 독화 레슨 · 발화 레슨 · 독화 요령 · 연습 · 과제 · 복습 · 분석 · 프로필 · 개발자 소개.
- 탭 안의 화면 축소본은 Figma에서 PNG로 내보내 이미지로 넣는다.
- 기존 `/guide` 페이지(`Guide.jsx`)는 이번 작업에서 삭제하지 않는다. 프로필 메뉴 동작만 모달 열기로 바꾼다.
- 9/25 구현(`components/GuideModal.jsx`): 탭 내용은 네 꼴이다. 01 화면 둘러보기는 축소본(338:274) 아래 주석 2열,
  02·03·04·06~10은 축소본(Mini screen 노드를 PNG 2배로 내보낸 `public/ui/lp-<노드>-guide-*.png`)과 오른쪽 주석
  (03·04는 축소본 2장에 작은 주석), 05 독화 요령은 번호 + 요령 + 예시 2열 3행(340:249), 11 개발자 소개는 팀원 5명 +
  소스 코드(342:530). 주석 문구는 모두 Figma 원문이다. 팀원 핸들은 깃허브 계정과 다를 수 있어 링크를 걸지 않고, 소스 코드만 링크다.

### 4-10. 로딩 · 에러: `components/LoadingScreen.jsx` · `components/ErrorScreen.jsx`
| Figma | 쓰임 |
|---|---|
| 256:34 / 256:48 기본 | 페이지 전환·데이터 로딩. 흰 배경 + DOKA + "로딩 중" + 점 3개 |
| 223:30 / 223:50 트랙별 | 레슨 시작 전. 트랙 색 그라데이션 + DOKA + 팁/TMI 문구 |
| 438:83 / 438:123 404 | 없는 주소(`App.jsx` `path="*"`의 `NotFound`). 눈이 X자인 DOKA + "페이지를 찾을 수 없어요" + 404 + 학습으로 돌아가기. 예전에는 학습 경로로 조용히 돌려보냈다 |
| 438:103 / 438:143 문제 발생 | `App.jsx` `ErrorBoundary`(청크 로드 실패 포함). 다시 시도(새로고침) · 학습으로 돌아가기. 설명 둘째 줄은 데스크톱에만 |

### 4-11. 연습 기능 — AppShell 안의 하위 페이지(사이드바 "연습" 활성)
| Figma | 라우트 · 파일 |
|---|---|
| 225:30 자유 발화 | `/pronounce` · `FreeSpeak`: 카드 하나(445:82): 입력 · 구분선 · 입모양 \| 성도 단면 2단 · 구분선 · 지금 발음 중 |
| 225:183 상황별 시나리오 (AI 대화) · 407:140 (문장 테스트) | `/learn/scenario` · `ScenarioHub`: 설정 카드 하나(447:82): 상황 입력 · 난이도 슬라이더(1~5 정수) · 연습 방식(문장 테스트 / AI 대화) → 카드 아래 시작하기(상황이 비면 비활성). AI 대화·여러 명이면 `/learn/conversation-multi?speakers=2~4&situation=`(인원은 나를 뺀 상대 수), 1:1이면 `/conversation` |
| 226:32 수어 보기 | `/learn/sign` · `Sign`: 입력 카드 + '수어' 영상 칸 하나 |
| 226:175 엔드리스 학습 | `/learn/endless` · `EndlessPractice` |
- 모든 연습 기능 화면은 제목 줄 오른쪽 끝에 나가기 X(453:83, 40px 원)를 두고 누르면 `/practice/hub`로 간다(`AppShell`의 `closeTo`). 제목 아래 부제는 없다.
- 다자 대화(옛 224:30)는 Figma에서 지워졌다. 라우트·파일(`/learn/conversation-multi`, `MultiConversation.jsx`)은 남아 상황별 시나리오의 'AI 대화 → 여러 명 대화'가 들어오는 화면이 됐고, 제목을 '여러 명 대화'로 바꿨다. 라우트·파일 삭제는 따로 정한다(변경 내역 §2).

### 4-12. 모바일 — 같은 라우트의 반응형
- `M / …` 프레임은 **별도 페이지가 아니다.** 위 라우트를 `lg` 미만에서 이렇게 보이게 한다.
- 대응: `M / 학습`(232:35)=58:11, `M / 독화 레슨`(235:34, 235:71)=91:12·94:98, `M / 발화 레슨`(236:34, 237:34)=175:21·176:21, `M / 연습·과제·복습·분석·프로필`(238:34, 238:160, 239:34, 240:34, 241:34), `M / 복습 (선택 모드)`(318:233), `M / 로그인·회원가입`(243:34, 244:34), `M / 자가진단 안내·문항·결과`(243:65, 385:120, 244:99), `M / 로딩`(243:81, 243:101).
- 모바일 레슨의 하단 버튼은 전체 폭이다.

---

## 5. 이동 흐름

```
[비로그인] / 랜딩 ─ 시작하기 ─▶ /signup · 계정이 이미 있습니다 ─▶ /login
[비로그인] Login ─ 로그인/데모 ─▶ (첫 방문) /onboarding ─ 시작하기 ─▶ /learn/placement ─ 결과 ─▶ /learn/path
                                                   └ 나중에 할게요 ───────────────────────────▶ /learn/path

/learn/path ─ 레슨 카드 "이어서 학습하기" ─▶ 레슨 집중 모드 ─ 완료 ─▶ 다음 레슨 / /learn/path
            ─ 잠긴 단계 "건너뛰기" ─▶ 확인 모달 ─▶ 해금 상태

사이드바/하단 탭 ─▶ /learn/path · /practice/hub · /tasks · /review · /analysis · /profile
/practice/hub ─ 카드 ─▶ §4-11 하위 페이지
/tasks ─ 배지 ─▶ 배지 모달
/review ─ 오답 복습 ─▶ /review/mistakes · 북마크 복습 ─▶ /review/saved · 지우기 ─▶ 선택 모드
/analysis ─ 버튼 3개 ─▶ 모달 · 회차 히스토리 행 ─▶ 회차 상세 모달
/profile ─ 사용법 가이드·계정 설정·학습 초기화 ─▶ 모달 · 자가진단 다시 하기 ─▶ /learn/placement
오른쪽 패널 "복습하기" ─▶ /review
```

---

## 6. 현재 코드와 Figma가 다른 점 (확인된 것)

1. **자가진단 화면(결정됨).** 코드의 `Onboarding.jsx`는 트랙 선택 화면(👁 이모지 포함)이고 `Placement.jsx`는 기존 배치검사 UI다. 배치검사 **로직은 유지**하고, 안내·문항·결과 화면을 Figma(84:7 · 385:82 · 85:9)로 교체한다. 자기 보고식 설문으로 바꾸지 않는다.
2. **연습 탭 카드.** 코드에는 "자가진단 다시 하기" 카드가 있으나 Figma에는 없다(프로필 메뉴로 이동). 카드를 제거하고 Figma의 5개 + 공사 중 카드로 맞춘다.
3. **프로필.** 코드에 로그아웃 버튼이 있으나 Figma에는 없다(계정 설정 모달 안으로 이동). 사용법 가이드는 `/guide` 이동이 아니라 모달을 연다. 학습 초기화 메뉴·모달이 없으므로 추가한다.
4. **과제 탭.** 배지를 눌렀을 때 여는 모달이 없으므로 추가한다.
5. **레슨 완료.** 독화 레슨 완료 상태가 없으므로 추가한다.
6. **남겨 둔 차이(9/25 전수 대조).** 모든 프레임의 글자 295개를 코드와 대조한 뒤 남은 것이다.
   - 1단계 입모양 인지 퀴즈 질문은 "이 입모양은 어느 그룹일까요?"다. 보기가 입모양 그룹이라서다. 91:12의 "어떤 글자일까요?"는 글자 보기용 틀이다.
   - 학습 배너의 단계·레슨 이름(Figma 예시: 입모양 익히기, 자음 + 모음 결합 등)은 백엔드 커리큘럼 데이터를 그대로 쓴다.
   - 오른쪽 패널 버튼은 "복습하기"(58:11)다. Figma 다른 프레임에는 "복습 시작하기"로 되어 있어 58:11을 따른다.
   - 가이드 개발자 소개의 염우진 역할은 Figma의 "개1발"(372:121)이 오타로 보여 "개발"로 쓴다.
   - 한글 문단은 어절 단위로 줄을 바꾼다(`break-keep`). Figma는 일부 문단을 음절 중간에서 끊는다.
   - 다자 대화는 9/26 변경 내역 §2로 결정됐다(연습 탭 진입점 삭제, 여러 명 대화로 대체). §4-11.
7. **사용자 요청으로 Figma에 없는 것을 더함(9/25).**
   - 랜딩 개발자 정보: 시작하기(48:56)와 푸터(9:56) 사이에 수상 표시("K-AI 공모전 B트랙 중고등부 대상 수상작"), 만든 사람들
     ("충남삼성고등학교 CNSAi 팀이 함께 만들었어요."), 팀원 카드 5장, 소스 코드 링크를 둔다. 팀원 정보는 가이드 11(342:530)과 같은
     `frontend/src/config/team.js`를 쓰고, 아바타는 `components/TeamAvatar.jsx`를 함께 쓴다.
   - 앱 아이콘: Figma에 탭·홈 화면 아이콘이 없어 로고(58:13)의 보라 캐릭터로 만든다(`scripts/build_app_icons.py`가 `logo.png`
     윤곽을 벡터로 따고 눈 두 점을 로고와 같은 자리에 둠). theme-color는 brand/primary(#7d53de). 예전 남색·하늘색 입술 아이콘은 뺐다.
8. **9/26 변경 내역(`LIPLAB_UI_CHANGELOG.md`) 적용.** 9/25 전수 대조 뒤 Figma에서 바뀐 곳(프레임 글자 재추출·가이드 축소본 재내보내기로
   확인)과 변경 내역의 구현 오류 항목을 반영했다.
   - 반영: 연습 탭 다자 대화 제거·수어 카드 '수어 보기'(§2·§4-4) · 연습 기능 화면 나가기 X와 부제 삭제 · 자유 발화 카드 하나(빠른 예시와
     '소리 내는 법' 목록은 Figma에서 빠져 지움, 성도 단면이 재생 중 입모양을 따라감) · 수어 보기(예시 칩·입모양 칸 삭제) · 상황별
     시나리오 전면 개편(§5) · 스크롤 안내를 버튼에서 장식으로(§0-1, `components/ScrollHintList.jsx`, 복습 목록 데스크톱 419·모바일 241,
     회차 상세 369, 목록 막대는 숨김) · 퀴즈 보기의 정답·오답 공개 상태 높이 보정(§0-4, 위 패딩 +2px) · 404·문제 발생 화면(§1) ·
     가이드 06 연습(다자 대화 주석 삭제, 축소본 346:77 다시 내보냄. 다른 축소본 10장은 바뀌지 않았다).
   - 이미 맞던 것: 워터마크(§3-1, Figma 좌표·회전·투명도 그대로), DOKA 경로 노드 4상태와 경로 옆 마스코트 없음(§3-2), 북마크 버튼·모달(§3-3·3-4),
     로그아웃 위치·프로필 메뉴 4개, 레슨 완료, 발화 상세 모달, 분석 그래프·회차 히스토리, 배치검사 화면, 가이드 모달 틀(§4).
   - 해당 없음: §0-2 버튼 라벨 줄높이. 버튼·칩의 줄높이는 모두 1.2 이상이고, Noto Sans KR은 1.2에서 받침이 깎이지 않는다(1에서 깎임).
     새로 만든 시나리오 선택 버튼은 Figma대로 1.5다. §0-3 북마크 버튼이 걸친 질문 줄에는 잘라내기가 없다.
   - 변경 내역과 Figma가 다른 곳(9/26 기준, 판단):
     - 상황별 시나리오의 'N단계 · 라벨'(1 아주 쉬움 ~ 5 아주 어려움), 양 끝 '짧고 쉬운 문장'·'길고 빠른 대화', 안내 두 줄('여러 명이면 누가
       말하는지도 함께 찾아야 해요', '나를 포함하지 않은 상대 수예요')은 변경 내역 문구다. Figma 225:183·407:140에는 'N단계'만 있고
       숨긴 글자도 없다. 변경 내역이 먼저라 넣었다.
     - 복습 북마크 카드 워터마크는 Figma 308:36이 30°다(변경 내역은 20°). Figma대로 둔다. 오답 카드는 20°.
     - 배지 상세의 희귀도 문구는 Figma 313:33이 "전체 사용자 중 N%가 획득했어요"다(변경 내역은 "상위 N%"). Figma대로 둔다.
     - 수어 카드는 데스크톱 96:14·기능 화면 226:32가 '수어 보기', 모바일 238:34와 가이드 06(340:323)은 '수어 함께 보기'다. 카드·화면
       제목은 '수어 보기'로, 가이드 주석은 그 프레임 글자 그대로 둔다.
   - 9/26 저녁 전체 검토 뒤 고친 것(디자인은 그대로): `ScrollHintList`는 필터를 바꾸면 맨 위로 되돌리고(`resetKey`), 목록이 빠지면
     부모에 '넘침 없음'을 알려 카드 아래 여백이 돌아온다. 스크롤이 생기면 틀이 키보드 초점을 받고(이름 '복습 항목'·'이 회차 문항'),
     초점 항목이 페이드에 가리지 않게 아래에 페이드 높이만큼 scroll-padding을 둔다. 목록 끝에서 바깥 스크롤로 이어지지 않던
     `overscroll-contain`은 뺐다. 상황별 시나리오 슬라이더는 어디를 눌러도 가장 가까운 단계로 간다(iOS Safari의 range는 끌어야만
     움직인다). 여러 명 대화에도 고른 난이도를 넘겨 한 턴 길이를 정한다(`?level=`, 1단계 4~8자부터 5단계 14~22자까지, 단계가 없으면
     3단계와 같은 6~14자). 자유 발화는 입모양 칸이 자리 그림으로 돌아갈 때 성도 단면도 쉼으로 돌린다.
   - 결정이 필요한 것: 엔드리스 화면의 '문맥으로 풀기' 카드(9/23 문맥 추론 진입점, Figma에 없음), 자유 발화 아래 음성구동 아바타(A4) 카드
     (Figma에 없음, 출시 기능이라 둠), `/learn/conversation-multi` 라우트·파일 삭제 여부, 상황별 시나리오 X의 목적지(변경 내역 §4-4대로
     늘 연습 탭으로 간다. 학습 경로 3·4단계 '학습 시작하기'로 들어와도 연습 탭으로 나가므로, 들어온 곳으로 돌아갈지 정해야 한다).
     이번 적용으로 쓰는 곳이 없어진 `SignPanel`의 두 칸(split) 변형과 `public/ui/card-multi.svg`는 위 결정을 할 때 함께 정리한다(지금은 둠).

---

## 7. 데이터 주의

1. **회차 히스토리.** 백엔드에 회차(세션)를 묶는 `session_id`가 없다(`Progress`, `SpeakAttempt`는 문항 단위). 1차 구현은 날짜·레슨 단위로 묶어 보여주고, 정답률은 독화 `Progress.score`, 발화 `SpeakAttempt.passed`로 계산한다. 회차 묶음이 필요하면 백엔드 변경이므로 멈추고 질문한다.
2. **회차 상세의 음소별 점수(해결).** 시도마다 음소 점수를 `SpeakAttempt.phones`에 저장하고(`[{label, dgop}]`, 최대 40개), 회차 상세(212:264)가 음소 칩 앞 6개를 보여 준다(`AnalysisTab.jsx` DetailItem). 칩은 Figma(176:36·182:88)처럼 자모 단위다. 자체 채점 모델(자모 어휘)이면 그대로 자모가 나오고, 공개 kresnik 모델(음절 어휘)이면 음절이 나온다.
3. **배지 희귀도.** "상위 N%"를 계산하는 API가 있는지 먼저 확인한다. 없으면 해당 줄을 숨기고 보고한다.

---

## 8. 구현 순서 (한 단계씩, 끝날 때마다 보고)

1. 토큰·공통 컴포넌트: 버튼(3D 아래 테두리), 카드, 모달, 워터마크 카드, `Bookmark button`, DOKA 노드
2. AppShell: 데스크톱 사이드바·오른쪽 패널, 모바일 상단 바·하단 탭
3. 학습 탭 상태 5개
4. 독화 레슨 템플릿(문제 → 정답/오답 → 완료)
5. 발화 레슨
6. 탭 페이지 5개 + 각 모달(배지, 선택 모드, 분석 모달 4개, 계정 설정, 학습 초기화)
7. 연습 기능 5개
8. 로그인 · 로딩 · 온보딩(자가진단 안내 · 문항 · 결과)
9. 사용법 가이드 모달

---

## 9. 작업 요청 예시

```
LIPLAB_UI_HANDOFF.md를 먼저 읽고 §0 규칙을 따라줘.
이번 작업: §8의 3단계 "학습 탭 상태 5개".
- 대상: /learn/path · CurriculumPath (새 라우트·페이지 만들지 말 것)
- Figma: node-id 58-11, 78-8, 79-5, 80-6, 171-38 (모바일 232-35)
- 각 노드를 get_design_context로 읽고 값은 추측하지 말 것
- 기존 StageGate·API 로직은 그대로 두고 UI만 바꿀 것
- 끝나면 Figma 스크린샷과 비교해서 다른 점을 목록으로 보고할 것
```
