# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
.venv/
ENV/
*.egg-info/
dist/
build/

# Database
*.db
*.sqlite
*.sqlite3

# Environment variables
.env
.env.local

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
frontend/dist/
frontend/build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*.swn
.DS_Store

# Logs
*.log
logs/

# Fly.io
.fly/

# Testing
.pytest_cache/
coverage/
htmlcov/
.coverage

# === 백업용 공통 제외 규칙 (자동 추가) ===
# === 자동 생성/의존성 (깃허브에 올리지 않음) ===
node_modules/
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/
dist/
build/
.next/
.expo/
.turbo/
.cache/
*.tsbuildinfo
coverage/

# === 환경변수·비밀정보 (.example 템플릿은 유지) ===
.env
.env.*
!.env.example
!.env.sample
*.key
*.pem
serviceAccount*.json

# === OS·에디터 ===
.DS_Store
Thumbs.db
*.log
.idea/
.vscode/

# === 축 G 생성 콘텐츠 후보(검수 전) — 승인본 approved.json만 별도 관리 ===
backend/data/curriculum/candidates_*.json

# 축 A 학습 산출물(체크포인트·vocab) — 용량이 커서 저장소에 넣지 않는다.
ckpt/

# 축 B 채점식 스윕용 특징 덤프(GPU 세션 산출물) — 용량이 크고 언제든 재생성 가능하다.
# 폴더 자체는 .gitkeep으로 남긴다(회수 시 scp 대상 폴더가 없으면 실패한다).
data_out/*.npz
# A4/audio2face 모델 가중치(대용량·torch 전용, 로컬/파드에서만 로드)
backend/models/*.pt

# 공개 자원 배포 묶음(scripts/build_resource_release.py로 언제든 똑같이 다시 만든다)
release/
