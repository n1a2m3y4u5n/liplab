# LIPLAB 개발일지 요약

> `DEVELOPMENT_LOG.md`(전체 상세 기록)의 핵심만 추린 요약본. 각 항목의 근거·검증 수치·
> 시행착오는 원본 로그를 참고.

---

## 1기 — 이전 대회 제출본 고도화 (K-AI 공모전용)

기준선(`c876854`)에서 출발, "새 기능보다 기존 기능 보강" 원칙으로 진행.

- **코드 정리**: 죽은 파일(`visemes$i.svg`, `Avatar3D.jsx`, 미사용 `avatar.vrm`) 제거
- **발음 변환(g2p) 엔진 보강**: 겹받침·연음·ㅎ탈락·무음 초성 ㅇ 처리 (`engine.py`, TDD 11 tests)
- **3D 모델 경량화**: 35.1MB → 6.3MB(82%↓), 무관 메시·텍스처 제거
- **viseme→ARKit 블렌드셰이프 정밀 매핑**: 모프타깃 실측 감사 후 재설계. 육안 검증 루프로 카메라 버그 1건 + 원순 입모양 부정확 2건 발견·수정
- **2D 폴백**: 3D 로드 실패 시 실제 입모양 SVG로 자동 전환
- **한국어→한국수어(KSL) 학습 보조 모듈 신설**: Claude gloss 번역 + 국립국어원 사전 실제 영상 인라인 재생 + 지문자(한글·영문) + 전역 텍스트 선택 번역
- **마감 다듬기**: three.js 지연로딩(초기 로드 385KB→130KB gzip), 데모 게스트 자동 로그인 도입

---

## 2기 — 「고도화계획서」 축 A~K 구현 (2026-08-25~, `feat/content-scale` 브랜치)

### Phase 0 — 로컬 즉시 착수 축

| 축 | 내용 |
|---|---|
| **G** 콘텐츠 대량화·개인화 | LLM 생성 + 규칙 게이트(동구형이음/최소대립) 이중 구조, 빈도·품사 필터, 지식추적(BKT 경량판) 개인화, 사람 검수 CLI |
| **J** 시각 증강 | 격음/경음/비음을 기식·긴장·울림 기호로 표시, 숙달도에 따라 자동 페이딩 |
| **D** 웹캠 채점 | MediaPipe FaceLandmarker 기반 코사인 유사도 채점 + 개인 캘리브레이션 |
| **I** 배치검사 | 규칙 기반 난이도 통제 배치검사 |
| **K** 얼굴 확장 단서 | 턱·볼·비강 미세움직임에서 비음·압력 보조 신호 추정 |
| **H** 다자 대화 | 2~3인 시나리오 생성 + 화자별 입모양 재생 |
| **D.5** | OLKAVS 전처리 파이프라인(ffmpeg만으로, 문장 단위 입술 ROI 클립) 검증 |

### Phase 1 — 백엔드 핵심 (규칙/로직 완성, 데이터·GPU 없이)

| 축 | 내용 |
|---|---|
| **B.1** D-GOP 핵심 로직 | 불확실성(엔트로피·여유값) 기반 과신 보정 + AV 후기 융합 — 순수 함수, 결정론적 테스트 |
| **B.3** | `/api/speak/assess`에 웹캠 입모양(D) 후기 융합 연결 |
| **C.1** 지각 자원 | 동구형이음 사전, 독화 난이도 지수 — 비심 규칙만으로 결정론적 도출, `docs/perceptual-resources.json` 공개 |
| **C.2** | 자음 시각 지각공간(MDS) — 양순/치경/연구개/경구개/성문 군집 재현 |

### Phase 2 착수 준비 (2026-09-07)

**병목**: B.2가 "음향 백본 — torch 필요"로 남겨뒀던 부분을 실제로 풀었다.

1. **ML 환경 구축** — `backend/requirements-ml.txt`(torch·torchaudio·transformers·librosa) 신규, 앱 런타임과 분리. `scripts/check_ml_env.py`로 다운로드→forward pass 검증.
2. **B.4 D-GOP 음향 백본** (`dgop_acoustic.py` 신규) — `torchaudio.functional.forced_align`으로 CTC 강제정렬 구현. 합성 log_probs로 결정론적 테스트.
3. **A.1 농인 발화 합성 프로토타입** (`deaf_speech_synthesis.py` 신규) — 말속도 신축·포먼트 이동(근사)·저역통과·잡음 추가, 5단계 저하 강도(`SEVERITY_LEVELS`). 합성 신호로 7개 테스트.
4. **B.5 D-GOP를 엔드포인트에 실제 연결** — `/api/speak/assess`에 `DGOP_MODEL_ID` 환경변수로 켜고 끄는 폴백 구조 배선(미설정 시 기존 전사 경로 그대로). 실제 HTTP 왕복으로 양쪽 경로 확인.
5. **C.3 데이터 기반 지각공간, 대조학습 메커니즘** (`perceptual_contrastive.py` 신규) — SupCon 손실 구현, 합성 클러스터로 학습 메커니즘 검증(라벨 분리 성공). **실채점에는 의도적으로 미배선**(합성 데이터 학습이라).
6. **B.6 한국어 체크포인트 확정** — `kresnik/wav2vec2-xlsr-korean`은 401(접근 불가) 확인, 같은 저자의 `kresnik/wav2vec2-large-xlsr-korean`(공개, 월 100만+ 다운로드)으로 `DEFAULT_MODEL_ID` 전환. **중요 발견**: 이 체크포인트 vocab은 자모가 아니라 **한글 음절 단위**(1,205토큰) — 자모별 채점을 얻으려면 음절→자모 재분해가 축 B 후속 과제로 남음.
7. **E.1 조음 시뮬레이터** (`vocal_tract_simulator.py` 신규) — VocalTractLab(오픈소스 물리 기반 성도 시뮬레이터) 연동. **학습되는 모델이 아니라 결정론적 시뮬레이터라 데이터 없이도 완성**. 한국어 viseme(1~10)을 실제 조음 위치 프리셋(양순 폐쇄·치경 폐쇄·연구개 폐쇄 등)에 매핑, `engine.text_to_visemes()`의 실제 출력을 그대로 흘려보내 "안녕하세요" 오디오를 실제로 합성(엔진↔시뮬레이터 첫 실연결).
8. **F.1 아바타 거울 모드** (`mouthMirror.js`·`MouthMirror.jsx` 신규) — 웹캠 입모양을 기존 아바타에 실시간 미러링. **구매 자산 없이 기존 `realistic_face.glb`만으로 완성**. GLB 모프 이름과 MediaPipe 출력이 둘 다 ARKit 표준이라 이름이 그대로 일치함을 실측 확인. MediaPipe 로딩을 공용 훅으로 뽑아 축 D와의 중복 제거(부수 효과로 공유 청크 분리).
9. **F.2 `visemeShapes.js` 중복 키 정리** — 문서에 "육안 검증 완료"로 기록된 입모양 튜닝이 중복 정의에 덮여 실행되지 않던 문제를 발견·수정(아래 상세). 소스 파싱 회귀 테스트로 재발 차단.

**오늘 신규/변경 파일**: `backend/requirements-ml.txt`, `backend/dgop_acoustic.py`, `backend/deaf_speech_synthesis.py`, `backend/perceptual_contrastive.py`, `backend/vocal_tract_simulator.py`, `backend/main.py`(D-GOP 배선), `scripts/check_ml_env.py`, `frontend/src/lib/mouthMirror.js`, `frontend/src/hooks/useFaceLandmarker.js`, `frontend/src/components/MouthMirror.jsx`, `AvatarVRM.jsx`·`WebcamMouthCheck.jsx`·`VisemeLiteracy.jsx`(연동), 각 테스트 5종. 백엔드 17개 파일·프론트 28개 테스트 전부 회귀 없음.

---

### Phase 2 실행 — 축 A 학습 (2026-09-08, RunPod H100)

**처음으로 GPU를 빌려 실제 모델을 학습시킨 세션.** 부트스트랩 데이터는 Zeroth-Korean
(51.6시간, CC BY 4.0, `kresnik/zeroth_korean`). 계획은 `docs/axis-a-training-plan.md`,
실행 절차는 `docs/axis-a-runbook.md`.

**계획서 원안을 바꿨다.** 3.1의 *"합성 저하 코퍼스로 미세조정"* 을 그대로 하면 D-GOP의
변별력이 사라진다 — 점수가 `naive × confidence`인데, 모델을 저하 발화에 강인하게 만들면
뭉개도 confidence가 올라가 점수가 높아진다. 요구가 정반대인 **두 모델로 분리**했다:
정렬기(저하에 강인, 구간 탐색)와 채점기(정상 발화 기준, 사후확률).

| 단계 | 내용 | 결과 |
|---|---|---|
| **A.3** 발음 규칙 | 평파열음화·유음화·비음화·경음화가 통째로 빠져 있었다. 기본(viseme) 경로는 그대로 두고 `phonetic=True`로만 켠다 | 14/14 |
| **A.4** 자모 CTC vocab | 음절 1,205토큰 → **자모 49토큰**. 처음 69로 잡았으나 평파열음화가 종성을 7종성으로 수렴시켜 줄었다 | 실제 Zeroth 전사 OOV 0건 |
| **A.5** 2모델 분리 | `aligner_id`/`scorer_id`, `DGOP_ALIGNER_ID`/`DGOP_SCORER_ID` | 합성 분포로 계약 고정 |
| **A.6** 학습·평가 스크립트 | A-1 Scorer · A-2 Aligner · A-3 변별력 평가 · 라벨 사전점검 | `--smoke`로 데이터 없이 형상 검증 |

**학습 실적** (H100 SXM, batch 16 / bf16 / workers 24)

| 단계 | 시간 | 결과 |
|---|---|---|
| A-0 라벨 전수점검 (22,263발화) | ~15분 | OOV 0건 / CTC 길이 위반 0건 |
| A-1 stage1 (head만, 3ep) | 5.8분 | eval_loss 0.348 → 0.322 |
| A-1 stage2 (전체, 12ep) | 59분 | eval_loss → **0.2087** |
| A-2 Aligner (4ep, severity 증강) | 20.8분 | eval_loss 1.299 → **1.147** |

**A-3 변별력 판정 — 축 A 합격**

| 지표 | 베이스라인 | 축 A | 목표 |
|---|---|---|---|
| 단조성 −ρ(sev, score) | 0.840 ❌ | **0.979** ✅ | 0.90 |
| 분리도 AUC(0 vs 3+) | 0.998 ✅ | **0.999** ✅ | 0.85 |
| 정렬 견고성(sev 4) | 1.000 ✅ | **1.000** ✅ | 0.95 |

베이스라인은 severity 3→4에서 D-GOP가 1.15 → **1.25로 올라가** 심한 저하를 구별하지 못한다.
축 A 모델은 9.51 → 2.74 → 1.49 → 0.61 → **0.30**으로 끝까지 단조 감소한다.

**GPU 2.5시간, $7~8** (추정 $15~20의 절반). 체크포인트 2개는 RunPod Network Volume에 보존.

**실행 중 잡은 문제 8건** — 전부 로컬 검증만으로는 안 드러난 것들이고, 이번 세션의 실질적
수확이다. 가장 위험했던 건 ①: 계획대로 `torchaudio>=2.9.0`을 깔았다면 **`forced_align`이
2.9에서 제거**돼 축 B 전체가 작동 불능이 된다.

| # | 문제 | 조치 |
|---|---|---|
| 1 | `forced_align`이 torchaudio 2.9에서 제거 예정 | `<2.9.0` 상한 (자체 구현이 후속 과제) |
| 2 | `torchcodec` 누락 + 최신판은 CUDA 13 요구 | 추가 후 `0.6.x` 고정 |
| 3 | datasets 5.x가 `AudioDecoder` 반환 | `backend/hf_audio.py` 신설 |
| 4 | PEP 668로 시스템 pip 차단 | `venv --system-site-packages` |
| 5 | `num_proc`이 224코어 그대로 | 상한 32 |
| 6 | `set_transform` + Trainer 컬럼 제거 충돌 | `remove_unused_columns=False` |
| 7 | 최적 체크포인트 유실 가능 | `load_best_model_at_end` |
| 8 | `ctc_log_probs`가 항상 CPU (GPU 0%) | `resolve_device()` |

**정직한 한계 둘.**
① **naive(표준 GOP)도 이 실험에서는 단조로웠다**(0.995 > D-GOP 0.979). *"표준 GOP는 뭉갠
발화에서 과신한다"* 는 전제가 합성 저하에 대해서는 성립하지 않았다 — 교란이 분포를 실제로
평평하게 만들어 naive도 함께 떨어진다. **실제 농인 발화로 검증해야 하며 그 데이터가 없다.**
② **점수 스케일이 압축돼 있다** — 깨끗한 발화가 9.51/100. 학습자에게 그대로 못 보여준다.
앱 연결 전 **점수 보정이 필수**다(로컬·무료).

부수 작업: 입모양 학습의 최소대립쌍 표시를 161쌍 → 예시 5개(● 3 + ○ 2)로 축소.

---

### Phase 2 후속 — D-GOP 점수 보정 (2026-09-09, 로컬)

위 **한계 ②** 해소. 변별력은 합격했는데 눈금이 사람 것이 아니었다 — 깨끗한 발화 9.51/100은
합격선(음소 50 / 단어·문장 65)과 비교조차 되지 않는다.

**원점수는 건드리지 않고 표시용 점수를 따로 만든다.** 채점식을 손대면 A-3에서 잰 변별력을
다시 증명해야 하기 때문이다. 응답에 `score`(보정)와 `raw_score`(원점수)를 함께 싣는다.

severity별 원점수를 앵커로 잡고 **log 공간에서 단조 보간**한다(원점수가 severity에 따라 대략
기하급수로 줄기 때문). 표시 목표는 `speak_curriculum` 합격선에서 역산한 제품 결정이다.

| severity | 0 정상 | 1 경도 | 2 중등도 | 3 중증 | 4 최중증 |
|---|---|---|---|---|---|
| 원점수(A-3 실측) | 9.51 | 2.74 | 1.49 | 0.61 | 0.30 |
| 표시 점수 | **90** | **72** | **58** | **40** | **20** |

**단조 증가 변환이라 A-3 변별력 지표(순위상관·AUC)는 보정 전후가 같다.** 변별력을 사후에
만들어내지 않고 있는 것을 읽을 수 있는 눈금으로 옮길 뿐이며, 이 성질은 테스트로 고정했다.
원점수가 severity를 거스르는 구간(베이스라인 3→4 역전)은 앵커에서 버린다 — 역전을 앵커로
삼으면 더 나쁜 발음에 더 높은 점수가 간다. 버린 severity는 출력에 남겨 숨기지 않는다.

앵커 우선순위 `DGOP_CALIBRATION` → `backend/data/dgop_calibration.json` → A-3 실측 내장값.
파일이 깨져도 내장값으로 떨어져 채점 자체는 죽지 않는다. 재적합 스크립트
(`scripts/fit_dgop_calibration.py`)는 A-3과 같은 채점 경로·시드를 쓰되 대표값은 평균이 아니라
**중앙값**을 쓴다(평균은 발화 몇 개의 극단값에 끌려가고, 앵커가 흔들리면 같은 발음에 다른
점수가 나온다).

**남는 한계.** 앵커가 **합성 저하** 기준이라 "severity 1 = 72점"이 실제 학습자 체감과 맞는지는
미검증이다(한계 ①과 같은 데이터 부재). 내장값은 **축 A 체크포인트 전용** — 모델을 바꾸면
반드시 재적합한다(런북 §6.5를 앱 연결 전 필수 단계로 넣었다).

---

### Phase 2 후속 — CTC 정렬 자체 구현, 그리고 구간 뭉갬 버그 (2026-09-09, 로컬)

계획된 작업은 **CTC 강제정렬 자체 구현**이었다(`torchaudio.forced_align` 의존을 덜어내는 축 B
후속 과제). 구현은 됐지만, 이 세션의 실제 수확은 그 과정에서 찾은 **기존 정렬 코드의 버그**다.

**버그 — 중복 출현 토큰의 구간이 뭉개진다.** 기존 `align_targets`는 정렬 경로를 토큰 id로
필터링해 `start=min(프레임), end=max(프레임)`으로 구간을 잡았다. 같은 토큰이 문장에 두 번 이상
나오면 **모든 출현이 '첫 출현 시작 ~ 마지막 출현 끝'이라는 하나의 거대한 구간**을 갖는다.
자모 vocab은 49토큰인데 Zeroth 라벨은 중앙값 103토큰이라 중복은 예외가 아니라 기본이다
(실측: 짧은 문장에서도 토큰의 20~57%가 중복 출현). 뭉개진 구간은 평균 분포가 평평해져
confidence가 0에 수렴하고 D-GOP가 통째로 깎인다.

합성 실험(또렷한 발화 23토큰)에서 구간 폭 중앙값 50 → 10프레임, 원점수 **46.5 → 95.2**.

**이것이 A-3 한계 ②(깨끗한 발화 9.51/100)의 실제 원인이다.** 어제 "CTC blank가 지배적인
프레임 탓으로 보인다"고 적은 추정은 틀렸다. 부수 피해로, 같은 토큰의 두 출현이 항상 같은
점수를 받아 음소별 피드백이라는 D-GOP의 취지도 무너져 있었다.

**자체 구현** (`backend/ctc_align.py`) — 확장열(2L+1) 위의 Viterbi. `token_spans`가 blank로
끊기는 연속 구간을 순서대로 목표열에 배정해 중복 출현을 분리한다. 정렬이 한 프레임만 밀려도
점수가 바뀌므로 **torchaudio와 같은 답을 내는지 직접 검증**했다 — 사례 6개 + 무작위 46회에서
프레임별 라벨·점수 전부 일치. torchaudio가 없으면 그 테스트만 건너뛴다.

**되돌려야 하는 것.** 체크포인트는 영향이 없지만(학습에 이 코드가 쓰이지 않는다) 측정값은
무효였다 — 같은 날 RunPod에서 재측정해 아래에서 확정했다.

**GPU 세션 보호.** `fit_dgop_calibration.py`는 커밋돼 있었을 뿐 **한 번도 실행된 적이 없었다**
(GPU·데이터가 필요해서). 배관 오류를 Pod에서 처음 만나면 20분·$1을 버린다. 그래서
`--smoke`(합성 발화·합성 채점기로 측정 루프~JSON 저장까지 그대로 통과, 프로젝트의
`train_*.py --smoke` 관례와 동일)를 넣고 로컬에서 통과시켰다. 앵커 적합이 실패해도 실측
중앙값을 `<out>.medians.json`에 남겨 **측정을 다시 하지 않게** 했다(실패 경로까지 확인).

**곁가지 — torchaudio 상한의 근거가 사라졌다.** 프로젝트 전체에서 torchaudio의 기능적
사용처가 없어졌고, 실측하니 `forced_align`은 **2.11.0에도 그대로 있다**("2.9에서 제거"는
2.8.0의 deprecation 경고를 보고 이르게 잡은 추정이었다). `requirements-ml.txt`의 `<2.9.0`
상한은 **그대로 두었다** — 푸는 것은 학습 재현성에 영향이 있어 별도 판단이 필요하다.

테스트: backend 20 → **21**(`test_ctc_align.py` 신설), scripts 2. 전부 통과.

---

### Phase 2 후속 — A-3 재측정·A-4 앵커 확정 (2026-09-09, RunPod H100)

버그 수정 후 같은 체크포인트로 §6 → §6.5를 다시 돌렸다. **재학습 없음.**
실제 소요: A-3 축 A 12분 + 베이스라인 9분 + A-4 11분. Pod 단가는 런북의 $2.99가 아니라
**$3.49/hr**이었다.

**A-3 — 축 A 합격, 버그가 원인이었음이 확정됐다**

| severity | 축 A (수정 후) | 축 A (버그) | 배수 | 베이스라인 (수정 후) |
|---|---|---|---|---|
| 0 정상 | **76.56** | 9.51 | 8.1× | 8.64 |
| 1 경도 | 31.26 | 2.74 | 11.4× | 5.01 |
| 2 중등도 | 15.56 | 1.49 | 10.4× | 2.68 |
| 3 중증 | 3.20 | 0.61 | 5.2× | 1.97 |
| 4 최중증 | 0.83 | 0.30 | 2.8× | **2.00 ↑** |

| 지표 | 베이스라인 | 축 A | 목표 |
|---|---|---|---|
| 단조성 −ρ | 0.813 ❌ | **0.998** ✅ | 0.90 |
| 분리도 AUC | 0.996 ✅ | **1.000** ✅ | 0.85 |
| 정렬 견고성 | 1.000 ✅ | **1.000** ✅ | 0.95 |

**한계 ②(스케일 압축) 해소.** 깨끗한 발화 9.51 → 76.56. "CTC blank 탓"이라는 추정이 아니라
구간 뭉갬 버그가 원인이었다.

**베이스라인의 3→4 역전은 버그 탓이 아니었다** — 수정 후에도 1.97 → 2.00으로 재현됐다.
2모델 분리 설계의 근거가 유지된다. 버그가 두 모델에 다르게 작용한 이유는 vocab 크기다:
음절 vocab(1,205)은 중복이 드물어 거의 안 다쳤고(6.65→8.64), 자모 vocab(49)은 중복이
기본이라 직격당했다(9.51→76.56). 버그의 성격과 정확히 맞는다.

**A-4 앵커 확정** — severity 중앙값 78.15 / 31.65 / 15.20 / 2.90 / 0.85, 5개 전부 채택
(`dropped_severities` 비어 있음). 중앙값이 A-3 평균과 근접해 극단값 쏠림도 없다.
`backend/data/dgop_calibration.json` 커밋. 내장 폴백도 같은 값으로 갱신.

**표시 점수 상한 94.9 (결정: 그대로 둔다).** 원점수 상한이 100인데 외삽된 최상위 앵커가 raw 129라
달성 가능한 최고 표시점수가 **94.9**다. 제품 결정이라 코드는 두고 테스트에 사실로 고정했다
(선택지 3개는 계획서 A-5 절 참고).

**한계 ①은 오히려 뚜렷해졌다.** 두 측정 모두 naive ≥ D-GOP이고, 베이스라인에서는 격차가
0.1로 벌어져 불확실성 보정이 단조성을 **깎았다**(naive 0.914 통과 / D-GOP 0.813 미달).
합성 저하에서는 보정이 중립이 아니라 손해다. 실제 농인 발화로만 판정된다.

**실기에서 걸린 것 하나** — Pod을 Start했는데 `PUBLIC_KEY` env가 비어 SSH가 전부 막혔다
(계정 키 등록과 무관, 프록시도 막힘). env에 공개키를 PATCH하고 재시작해 뚫었다. PATCH가
컨테이너를 재시작시켜 포트 매핑이 또 바뀌는 것까지 런북 §2.1에 적었다.

---

### Phase 2 후속 — 농인 발화 데이터 조사, 그리고 채점식 결함 발견 (2026-09-09)

축 A의 한계 ①(실제 농인 발화 부재)을 풀려고 데이터 확보 경로를 7개 축으로 병렬 조사했다.
결과는 `docs/deaf-speech-data-research.md`. **조사가 답한 것은 원래 질문이 아니었다.**

**핵심 발견 — 데이터가 1차 병목이 아니다.** 채점식 `D-GOP = naive × confidence`에 구조적
결함이 있고 데이터 없이 증명된다(`scripts/analyze_dgop_redundancy.py`):

- **ρ(naive, confidence) = 0.980.** 두 항이 같은 신호(분포 첨도의 단조 함수)라 곱셈이 순위
  정보를 더하지 못하고 잡음만 더한다 → `-ρ(severity, score)`가 **구조적으로** naive 이하가
  된다. A-5 실측(1.000→0.998, 0.914→0.813)이 정확히 그 예측값이다.
- **보정 방향이 반대다.** 과신은 분포가 *뾰족한데* 틀린 것인데, confidence가 재는 것이 그
  뾰족함이라 과신을 통과시킨다(A: naive .950 → dgop .811). 실제로 걷히는 것은 **과소확신**이다.

즉 **실제 농인 발화를 구해도 이 형태로는 과신을 못 잡는다.** A-5에 적은 "합성이라서
재현되지 않았다"는 진단은 절반만 맞았다.

**선행 연구가 같은 실험을 한국어로 이미 했다.** Yeo, Choi, Kim, Chung (Interspeech 2023,
서울대, 코드 MIT). 구음장애 발화 한국어 Kendall τ — 베이스라인 −0.524 / **엔트로피 −0.264** /
**마진 −0.443** / Prior+MaxLogit −0.544. **우리가 곱한 두 신호가 그 표에서 가장 나쁜 둘**이고,
이긴 것은 softmax를 탈출하는 MaxLogit뿐이다(상대 개선 3.91%로 크지도 않다).

**초판의 오진 하나 정정** — *"span_distribution이 blank 프레임을 구간 평균에 포함한다"*고
적었으나 틀렸다. `ctc_align.token_spans`가 비blank run만 구간으로 잡으므로 blank가 원리적으로
들어가지 않는다(실측 확인). 지적 대상이었던 것은 **A-5 이전 구현**이고, 구간 뭉갬 버그를
고치면서 함께 사라졌다. 불변 검사와 테스트로 고정했다.

**데이터 조사 결과 요약**

| 구분 | 자원 |
|---|---|
| 지금 받을 수 있음 | **TORGO**(토론토대 무료 — LDC $1,200이 아니다, HTTP 200·9.58GB 확인), **speechocean762**(음소별 전문가 라벨, CC BY 4.0 상업 가능), **SPAL 메타데이터**(농인 34화자 라벨 CSV, 오디오 없음) |
| 절차 필요 | **AI Hub 608**(언어청각 30.35%≈1,520h, 3등급 중증도, **본인인증만으로 다운로드** — 안심존 아님을 원본 HTML의 `s3FileCnt`로 확정), **AI Hub 71434**(청각장애 210만 건 + 말명료도 + 선천/후천 + 발음·철자 전사 쌍, 안심존+IRB), COPAS(농인·구음장애를 같은 척도로), QoLT |
| 전수 확인된 공백 | 국립국어원(138종) · 공공데이터포털 · ETRI(운영종료) · SiTEC(소멸) · ELRA · OpenSLR · Zenodo/OSF/HF — 전부 0건 |
| 기각 | 유튜브 추출(명료도 상위 편중으로 **과학적으로 성립 안 함** + TDM 면책 부재 + 장애정보는 민감정보), 자체 수집(검정력상 화자 **150명** 필요, 9~15개월·1,000만원+) |

**가장 중요한 연결**: Yeo et al.의 Kim·Chung = SNU 인공와우 코퍼스 = QoLT 접근 경로가
**같은 연구실**(서울대 정민화)이다. 접촉 1순위.

**우리 쪽에서 고쳐야 할 것** — 실데이터 투입 전 선결: ① 채점식
③ 유의성 검정·CI 부재(naive 0.914 vs D-GOP 0.813에 검정이 안 붙어 있다) ④ AUC가 발화 단위
pooling(화자 단위여야 한다) ⑤ 합격선 0.90이 감쇠 때문에 도달 불가일 수 있음 — 재정의가
데이터보다 먼저 ⑥ 모집단 전제(청각장애인의 84.2%가 주 의사소통 수단이 "말"이고 대다수는
발화가 정상이다. 대상은 **언어습득 전 고도–심도 난청**이라는 작은 하위집단).

⑥과 관련해 **청력손실 dB를 중증도 대리 지표로 쓰려던 판단은 철회한다** — dB는 "듣기"의
지표이지 자기 발화 명료도가 아니다(후천성 난청 성인은 90dB HL이어도 발화가 멀쩡하다).

---

### Phase 2 후속 — 채점식 재설계 배관 (2026-09-09, 로컬)

채점식 결함(위 절)을 실측으로 판정할 실험 두 개의 **로컬 선행 작업**을 끝냈다.
계획·실행 절차·판정 기준은 `docs/axis-b-scorer-redesign.md`. **GPU 실행만 남았다.**

핵심 설계는 **GPU를 한 번만 쓰는 것**이다. 순전파 결과(평균 확률·로그확률·raw 로짓)를
npz로 뜬 뒤 채점식 비교는 순수 함수 replay로 돌린다 — 채점식을 추가할 때마다 Pod을
다시 빌리지 않는다.

| 파일 | 역할 |
|---|---|
| `backend/gop_variants.py` | 문헌 표준 GOP 변형 9종(Witt & Young 2000 / Hu et al. 2015 / Yeo et al. 2023) |
| `backend/jamo_perturb.py` | 목표 자모열 오염 4종(종성 탈락·마찰음 파열음화·비음 대치·모음 중앙화) |
| `scripts/dump_gop_features.py` | 구간 통계 덤프 — 유일한 GPU 단계 |
| `scripts/sweep_gop_scorers.py` | 채점식 일괄 비교 + **발화 단위 부트스트랩 CI** |

E2(과신 측정)는 **오디오를 건드리지 않는다.** 깨끗한 발화를 그대로 두고 목표 자모열만
오염시킨다. 과신은 '음향은 q인데 P(p)를 높게 준다'는 현상이라, 목표만 틀리게 하면 저하
발화 합성 없이 그 조건이 만들어진다 — 합성 저하가 못 만든 '체계적 치환'이 이것이다.

**작업 중 확인된 것 셋.**

① **D-GOP가 순위를 뒤집는다**(새 발견). '또렷하게 다른 음소를 발음함'(0.0128)이 '머뭇거리지만
목표가 1등'(0.0015)보다 **8배 높은 점수**를 받는다. naive는 이 순서를 맞게 매긴다. 확신에 차서
틀릴수록 점수가 오른다는 뜻이라 발음 교정 앱에서는 치명적이다. 테스트를 약화시키지 않고
회귀 테스트로 고정했다(채점식을 고치면 단언을 뒤집는다).

② **blank 집계 결함은 내 오진이었다**(앞 절의 서술을 정정). `ctc_align.token_spans`가 비blank
run만 구간으로 잡아 구간 안에 blank가 원리적으로 0개다. 문헌 지적이 겨눈 것은 A-5 **이전**
id 필터링 구현이었고 구간 뭉갬 수정으로 함께 사라졌다. 에이전트 보고를 수정 후 코드에
대조하지 않고 옮긴 것이 원인 — 불변 검사와 테스트로 재발을 막았다.

③ **`nn_gop` 포화.** 경쟁 음소에서 목표를 빼지 않으면 목표가 argmax인 순간 0으로 포화해
'아슬아슬한 1등'과 '압도적 1등'을 구별하지 못한다. 정상 발화 대부분이 그 구간에 있다.

**GPU 세션 보호**: `--mode perturb`는 자모 vocab 전용인데 음절 vocab으로 돌리면 오염이 하나도
안 걸려 clean만 쌓인다. 12분 뒤에 알게 되므로 첫 발화에서 즉시 실패하도록 막았다. 체크포인트가
없어 로컬 검증이 불가능한 perturb 경로는 순전파를 가짜로 바꾼 테스트로 덮었다.

테스트: backend 20 → **22**, scripts 2 → **3**. 전부 통과.

⚠️ **실행의 최대 리스크는 GPU 확보다.** AP-IN-2에 H100이 없어 Pod 재시작이 24회 실패한
상태다(런북 §7.1). **켜는 데 성공하면 그 세션에서 체크포인트 백업 → E1 → E2를 전부 끝낸다** —
끄면 다시 못 켤 수 있다.

---

## 지금 남은 것 (다음 단계)

- ~~**D-GOP 점수 보정(calibration)**~~ — **해결됨**(2026-09-09). 원점수는 두고 표시용 점수를 따로 만든다(위 참고).
- ~~**A-3 재측정 + 앵커 재적합**~~ — **완료**(2026-09-09). 축 A 합격(단조성 0.998), 앵커 커밋됨. 앱 연결의 선결 조건이 풀렸다.
- ~~**100점 도달 불가 결정**~~ — **결정됨**(2026-09-09): 그대로 둔다. 표시 점수 상한 94.9이고 만점은 나오지 않는다 — 발음 교정 앱에서 만점을 주지 않는 편이 낫다는 판단. `test_dgop`이 이 성질을 고정한다.
- **앱 연결** — `DGOP_ALIGNER_ID`/`DGOP_SCORER_ID` 설정. 체크포인트가 RunPod 볼륨에만 있어 HF 백업이 선행돼야 한다.
- **채점식 재설계 (E1) + 과신 측정 (E2)** ⭐⭐ — **배관 완료, GPU 실행만 남음**(2026-09-09, `900bb1f`). 계획·명령어·판정 기준은 `docs/axis-b-scorer-redesign.md`에 전부 적어 뒀다.
  - 남은 일: Pod에서 덤프 2회(~25분, ~$2) → npz 회수 → 로컬에서 스윕(GPU 불필요)
  - 판정이 배타적이라 어느 결과든 답이 된다: *Prior+MaxLogit이 이기면* 형태만 틀린 것, *아무것도 naive를 못 이기면* UQ 자체가 무의미, *dgop이 유의하게 나쁘면* 즉시 교체
  - **GPU 확보가 선결**(아래 체크포인트 백업과 같은 병목). 켜지면 백업 → E1 → E2를 한 세션에 끝낸다
- **실제 농인 발화 확보** — 여전히 필요하지만 **1순위가 아니다**(위 둘이 선행). 경로는 `docs/deaf-speech-data-research.md` 참고. 즉시 가능: TORGO·speechocean762로 대리 검증. 한국어: AI Hub 608 샘플 확인 → 언어청각 파트.
- ~~**CTC 강제정렬 자체 구현**~~ — **해결됨**(2026-09-09, `backend/ctc_align.py`). torchaudio와 프레임 단위 일치 검증 완료. 남은 판단: `requirements-ml.txt`의 `torchaudio<2.9.0`·`torch<2.9.0` 상한을 실제로 풀 것인가(기술적 근거는 사라졌으나 학습 재현성에 영향).
- **축 A 체크포인트 백업** — **미완, 보류**(2026-09-09). RunPod Network Volume(60GB, 월 $4.20)에만 있다.
  - 된 것: HF private repo 2개(`duadnwls/liplab-dgop-{scorer,aligner}`) 생성 + 모델 카드 업로드(출처·학습·변별력·한계·"Scorer를 저하 발화에 강인하게 만들면 안 된다" 제약 명시). 업로드 스크립트 `scripts/upload_ckpt_hf.py`(최종 모델만, 2.5GB, 업로드 후 크기 대조 검증).
  - 막힌 것: **AP-IN-2에 H100이 안 비어 Pod을 다시 켜지 못했다**(90초 간격 24회 실패). 그 DC엔 H100 한 종류만 있고 CPU 인스턴스도 없어 우회로가 없다. 원인은 **Pod 정지 시 GPU 예약이 풀리는 것** — 런북 §7.1에 기록.
  - 결정: 볼륨은 **그대로 둔다**(하루 $0.14). 나중에 GPU가 비면 스크립트 두 줄로 끝난다. 잃어도 $7~8·1.5시간이면 재학습되지만, 그러면 A-3·A-4 재실행(+40분·+$2)과 앵커 재적합이 따라온다.
- **OLKAVS 등 실 데이터 확보** — 사용자 쪽 액션(AI Hub 본인인증 필요). 도착하면 A-1을 더 큰 데이터로 재학습하고, `perceptual_contrastive.py`를 실제 (특징, 비심라벨) 데이터로 재학습(축 C)
- ~~**음절→자모 재분해**~~ — **해결됨**(A.4). 미세조정 시 `lm_head`를 자모 49토큰으로 교체해 B.6 한계가 함께 풀렸다.
- **축 D 자체 립리딩 모델** — 아직 미착수(웹캠 실시간 채점은 완료, 립리딩 인식 모델 학습은 별개), OLKAVS 필요
- **축 E 조음 역추정** — 시뮬레이터(E.1)는 완료, 나머지 절반(음향/영상→조음 파라미터 역추정)은 MNGU0·USC-TIMIT 데이터 필요
- **축 F 거울 모드 육안 검증** — F.1은 구현·테스트 완료지만 이 개발 환경에 카메라가 없어 실제 웹캠으로 아바타가 따라 움직이는지 확인 못 함. 카메라 있는 기기에서 확인 필요(특히 증폭 계수 `gain` 적정성)
- **축 F 투명 두상·실사 아바타** — 구매 대기(Character Creator 5·Headshot 3·3D 성도 모델). TalkingHead 립싱크 연동은 구매 없이 가능(다음 후보)
- **`/dev-viseme` 육안 재검증** — F.2 중복 키 정리로 복구·병합된 입모양 값들을 브라우저에서 확인 필요(아래 참고)
- **당사자 파일럿** — 협력기관 섭외 필요(사람 대 사람 커뮤니케이션)

---

## 발견·수정한 기존 결함 (F.2)

### `visemeShapes.js` 중복 키로 입모양 튜닝이 무효화되던 문제

축 F 작업 중 발견. `VISEME_BLENDSHAPES`에 viseme **1·2·4·6·7·10이 각각 두 번 정의**되어
있었고, JS 객체 리터럴은 뒤엣것이 앞엣것을 덮으므로 앞의 정의가 **코드에 남은 채 실행되지
않고** 있었다. 덮여 죽은 쪽이 하필 개발일지 2·6절이 "ARKit 감사 + 브라우저 육안 검증으로
확정"으로 기록한 값이었다 — 즉 **6절이 스크린샷으로 잡아낸 원순모음 교정이 화면에 반영된
적이 없었다.**

**해결.** 두 정의가 서로 다른 축을 손대고 있음을 확인해 병합했다:
- 앞 정의 = **입술 형태**(감사·육안 검증)
- 뒤 정의 = **턱 열림** — jawOpen을 올린 대상 6·7·10이 `VISEME_TONGUE` 보유 viseme과
  정확히 일치 → 혀를 보이게 하려는 혀 렌더링 작업의 의도

→ 입술은 앞, 턱은 뒤를 취해 단일 정의로 통합(파일 헤더가 말한 "두 기능을 모두 살려 단일화"를
실제로 수행). 런타임으로는 중복을 볼 수 없으므로 **소스를 파싱하는 회귀 테스트**를 추가했고,
일부러 중복을 주입해 테스트가 실제로 잡아내는지까지 확인했다.

**남은 확인.** 브라우저가 없는 환경이라 `/dev-viseme` 육안 재검증은 못 했다. 되살린 값 자체는
6절이 이미 검증한 것이지만, 병합으로 새로 생긴 조합(치경음 `jaw .22 + mouthUpperUp`,
경구개음 `jaw .18 + funnel .14`)은 실제 렌더에서 확인이 필요하다.
