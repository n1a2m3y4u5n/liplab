# LIPLAB 개발일지 (Development Log)

> 이 문서는 **K-AI 콘텐츠 공모전(솔루션 부문)** 제출을 위해 LIPLAB을 고도화하는
> 전 과정을 순서대로 기록한다. 공모전 신청서 **(3) 개발과정 — "프롬프트 포함한
> 모든 과정을 순서대로 작성"** 항목의 근거 자료로 사용한다.
>
> 각 작업 단위는 다음 형식으로 기록한다:
> **배경/목표 → AI에게 내린 지시(프롬프트) → 실행 내용 → 검증 → 변경 파일**

---

## 0. 프로젝트 배경과 기준선(baseline)

### 0.1 무엇을 만드는가
LIPLAB은 **청각장애인을 위한 AI 기반 한국어 독화(讀話, speechreading) 훈련 웹앱**이다.
한국어 문장을 입력하면 실제 발화와 유사한 3D 입모양 애니메이션이 생성되고,
사용자는 이를 반복 학습하며 입모양만으로 말을 알아듣는 능력을 기른다.

- 배포 URL: https://liplab.fly.dev
- 기술 스택: FastAPI(백엔드) + React 19/Vite/Three.js(프론트엔드) + Anthropic Claude API + Fly.io

### 0.2 기준선 = 이전 대회 제출본
현재 코드(단일 커밋 `c876854`)는 **2026 제4회 IT코딩 발명 아이디어 및 에세이 경진대회**에
제출했던 버전과 동일하다. 이번 K-AI 공모전에서는 이 버전을 **출발점(before)**으로 삼아
고도화하고, 무엇이 어떻게 달라졌는지를 명확히 제시한다.

**이전 버전이 이미 갖춘 것 (건드리지 않아도 되는 자산):**
- 한국어 음절 → 초성/중성/종성 분해 → **10개 입모양 그룹 + 15종 비심(viseme) 상태** 변환 엔진 (`backend/engine.py`)
  - g2pk 없이 자체 음운 규칙(연음·격음화·ㅎ탈락) 구현
  - 음소 타입별 실측 기반 지속시간, 동시조음(co-articulation) 전환 프레임 포함
- **Claude API 적응형 시나리오 생성** — 8개 실생활 상황 + 사용자 입력 상황, 취약 입모양 가중치 (`backend/llm_service.py`)
- **자모 단위 음운론적 유사도 채점** — 초성30/중성50/종성20 가중, 시각적 혼동쌍 유사도 계수 (`backend/scoring.py`)
- 학습 모드 5종: 학습 / 주관식 테스트 / 4지선다 / 복습 / 대화 연습
- 게이미피케이션: XP·레벨·연속학습 스트릭, 90일 캘린더 히트맵, 북마크
- 분석 페이지: 입모양별 평균점수, 음소 혼동 통계, AI 맞춤 권고
- 3D 립 애니메이션: `realistic_face.glb`(ARKit+Oculus 비심 모프타깃 65종 내장)

### 0.3 기준선의 약점 (이번에 개선할 지점) — 코드 정밀 진단 결과
1. **3D 모델이 36MB 전신 모델**을 마우스에 클로즈업하는 구조 → 저사양·불안정 통신망에서 로딩 부담.
2. **죽은 코드/버그**:
   - `frontend/public/visemes$i.svg` : 셸 스크립트 변수(`$i`) 미치환으로 커밋된 쓰레기 파일.
   - `LipSyncPlayer3D`가 `modelUrl="/models/avatar.vrm"`를 넘기지만 `AvatarVRM`은 이를 **무시하고** GLB를 하드코딩 → prop이 죽어 있음. 1.3MB VRM은 미사용.
   - 2D 폴백 SVG(`1.svg`~`15.svg`)가 **"숫자가 적힌 색깔 네모"** → 독화 학습에 무의미(3D 로드 실패 시 학습 불가).
3. **viseme→모프 매핑이 거칠다**: GLB에 `jawOpen`/`mouthPucker`/`mouthFunnel`/`mouthClose` 등 정밀 ARKit 셰이프가 있는데 코드는 15개 Oculus 비심만 대충 혼합 사용 → 한국어 입모양을 더 정확히 표현할 여지가 큼.

### 0.4 이번 고도화의 목표 (요약)
> "입을 보고 말을 읽는" 핵심 경험을 **더 정확하게, 더 가볍게, 그리고 양방향으로** 만든다.
> 세부 방향은 아래 개발 계획 참조. 모든 단계는 이 문서에 순서대로 기록한다.

### 0.5 개발 도구
- **Claude Code (Opus 4.8)** — 코드 분석·설계·구현·검증 전반 (AI 페어 프로그래밍)
- **Anthropic Claude API** — 앱 런타임에서 학습 시나리오·대화·분석 권고 생성
- 개발 환경: macOS, Node.js/Vite, Python/FastAPI

---

## 작업 기록 (시간순)

<!-- 이 아래로 각 작업을 순서대로 추가한다 -->

### [2026-07-08] 작업 #1 — 기준선 확보 및 코드베이스 진단

**배경/목표.** 고도화에 앞서 이전 대회 제출본이 실제로 빌드·동작하는지 확인하고
(before 상태 확정), 개선 지점을 코드 레벨에서 정밀 진단한다.

**AI에게 내린 지시(요지).**
> "GitHub 저장소 liplab를 클론해서 전체 구조를 파악하고, 독화 학습 앱으로서
> 가장 부실한 지점과 개선 우선순위를 진단해라. 특히 3D 입모양이 실제로
> 동작하는 구조인지 확인해라."

**실행 내용.**
- 저장소 클론 → 백엔드(FastAPI, 7파일 2,066줄) / 프론트(React, 페이지 7 + 컴포넌트 6) 전수 분석.
- 3D 모델 `realistic_face.glb`의 모프타깃을 스크립트로 추출 → **ARKit 52종 + Oculus 비심 14종** 내장 확인(입모양 애니메이션 가능함을 검증).
- 컴포넌트 의존 관계 추적 → 실사용 경로(`Practice`→`LipSyncPlayer3D`→`AvatarVRM`→GLB)와 죽은 코드 식별.

**검증.**
- `npm install && npm run build` → **성공**(3.49s). three 벤더 청크 322KB gzip + GLB 36MB 별도 로드 확인.

**결과(진단).** 위 `0.3 기준선의 약점` 항목으로 정리. 핵심: ①36MB 모델 로딩 부담,
②죽은 코드/버그 3건, ③viseme→모프 매핑이 거침.

---

### [2026-07-08] 작업 #2 — 코드 정리(surgical): 쓰레기 파일·죽은 prop 제거

**배경/목표.** 어떤 개발 방향을 택하든 무해하고 확실히 이득인 정리부터 수행.
karpathy 가이드라인(#3 surgical changes)에 따라 **명백한 것만** 최소 변경.

**실행 내용.**
1. `frontend/public/visemes$i.svg` 제거 — 셸 스크립트 변수(`$i`) 미치환으로 잘못 커밋된 파일.
2. `LipSyncPlayer3D.jsx`에서 `AvatarVRM`에 넘기던 `modelUrl="/models/avatar.vrm"` prop 제거
   — `AvatarVRM`이 이 값을 무시하고 GLB를 하드코딩하고 있어 **죽은 prop**이었음.

**아직 손대지 않음(방향 확정 후 처리):** 완전히 죽은 컴포넌트
`components/LipSyncPlayer.jsx`(2D), `components/Avatar3D.jsx`(구체 프로토타입),
미사용 `public/models/avatar.vrm`, `public/visemes/*.svg` placeholder.
→ Phase 3 방향(3D 유지 vs 경량 2D 재구축)에 따라 재활용/삭제 결정.

**변경 파일.** `frontend/public/visemes$i.svg`(삭제), `frontend/src/components/LipSyncPlayer3D.jsx`.

---

### [2026-07-08] 작업 #3 — 코어 정확도: viseme→입모양 매핑 재설계 (ARKit 기반)

**배경/목표.** 기존 3D 입모양은 Oculus 비심(`viseme_aa`, `viseme_PP` 등) 15종만
사용했다. 모델(`realistic_face.glb`)에는 훨씬 정밀한 **ARKit 표준 블렌드셰이프**
(`jawOpen`, `mouthPucker`, `mouthFunnel`, `mouthSmile`, `mouthPress` 등)가 함께
들어 있으므로, 이를 활용해 한국어 조음(원순/개방/폐쇄/전설)을 더 정확히 표현한다.
독화(입모양 읽기) 앱에서 입모양 정확도는 **제품의 핵심 그 자체**다.

**AI에게 내린 지시(요지).**
> "GLB의 모프타깃을 추출해 어떤 블렌드셰이프가 있는지 확인하고, 한국어
> 조음음성학에 근거해 viseme 1~15를 정확한 입모양으로 매핑해라. 각 입모양을
> 브라우저에서 실제로 렌더링해 눈으로 검증하고, 이상하면 고쳐라."

**실행 및 시행착오(순서대로).**
1. **1차 시도 — ARKit + Oculus 혼합.** viseme별로 `viseme_*`를 주로 쓰고 ARKit
   셰이프를 높은 가중치로 얹었다(예: 양순 = `viseme_PP:0.9 + mouthClose:0.6`).
2. **검증용 임시 페이지(`/dev-viseme`)를 만들어** viseme 1~15를 버튼으로 전환하며
   3D 입모양을 스크린샷으로 확인.
3. **문제 발견 ①:** 양순음(1)에서 **메시가 깨짐**(입술이 사다리꼴로 찢어지고 모서리
   지오메트리가 튀어나옴). 원인 추적 결과 **이 모델의 `viseme_PP`(및 다른
   `viseme_*`) 모프 자체가 부실**하게 제작되어 있어, 단독으로도 메시를 왜곡시켰다.
4. **문제 발견 ②(HMR 함정):** 매핑에서 키를 제거하면, 이미 적용된 모프 값이
   프레임에 남아 0으로 복귀하지 않는 현상. (매 프레임 "사용 키 목록"만 보간하는데,
   목록에서 빠진 키는 아무도 0으로 되돌리지 않음.) → 검증 시 페이지 새로고침으로 해결.
   실제 재생에서는 사용 키 목록이 로드 시 1회 고정되므로 문제없음을 확인.
5. **2차 설계 — ARKit 전용으로 전환.** `viseme_*`를 **전면 배제**하고, 표준이라
   안정적인 ARKit 블렌드셰이프만 조합. rest(기본) 자세가 이미 '입술을 편하게 다문'
   상태이므로 여기서 필요한 만큼만 벌리고(`jawOpen`)/오므리고(`mouthPucker`)/
   당기는(`mouthSmile`) 방식.
6. **재검증(스크린샷).** 핵심 입모양 확인:
   - **양순 ㅂㅍㅁ(1):** `mouthPress+mouthRoll` → 입술 깔끔히 다물고 살짝 압착 ✓
   - **개방 ㅏㅐ(2):** `jawOpen:0.42` → 턱 내려 크게 벌림 ✓
   - **전설 ㅣㅔ(3):** `mouthSmile:0.5` → 입술 좌우로 당겨 벌림 ✓
   - **원순 ㅗㅜ(4):** `mouthPucker:0.95` → 입술 앞으로 오므려 둥글게 ✓
   - 4개가 **시각적으로 명확히 구분**됨(독화 학습의 핵심 요건).

**검증 결과(before → after).**
- before: 양순음에서 메시 왜곡, 원순모음이 둥글지 않고 이가 드러남(부정확).
- after: 왜곡 제거, 닫힘·개방·전설·원순이 뚜렷이 구분되는 정확한 입모양.
- `npm run build` 성공.

**변경 파일.**
- 신규 `frontend/src/lib/visemeShapes.js` — 한국어 viseme→ARKit 블렌드셰이프
  정밀 매핑 + 사용 키 목록. **3D뿐 아니라 향후 웹캠 채점의 '정답 입모양' 기준으로
  재사용**할 조음 모델.
- `frontend/src/components/AvatarVRM.jsx` — 위 매핑을 import하도록 교체
  (기존 인라인 `KOREAN_TO_OCULUS` 삭제).

**핵심 인사이트(문서화 가치).** "정밀한 모프타깃이 있다고 다 좋은 게 아니다 —
모델마다 모프 품질이 다르다. 실제 렌더링을 눈으로 검증하지 않았다면 깨진
입모양을 그대로 배포할 뻔했다." 육안 검증 루프(dev 페이지 + 스크린샷)가 결정적이었다.

---

### [2026-07-09] 방향 전환 — "신규 기능"이 아니라 "기존 기능 보강"으로

**의사결정 기록.** 헤드라인 신기능(웹캠 등)을 검토했으나, 팀 논의 결과
**"새 기능 추가보다 기존에 있는 것부터 보강"** 하기로 방향을 확정했다.
근거: 독화 앱의 생명은 '정확한 입모양'인데, 렌더링(Phase 2)을 아무리 다듬어도
**입력되는 발음 자체가 틀리면 무의미**하다(garbage in, garbage out). 따라서
가장 근본인 발음 변환 엔진부터 보강한다.

### [2026-07-09] 작업 #4 — 발음 변환(g2p) 엔진 보강 (연음·겹받침·ㅎ탈락·무음 초성)

**배경/목표.** 독화는 '철자'가 아니라 '소리 나는 대로'의 입모양을 보여줘야 한다.
기존 엔진의 발음 변환은 **사실상 동작하지 않았다.**

**AI에게 내린 지시(요지).**
> "현재 엔진이 실제 발음을 얼마나 정확히 입모양으로 바꾸는지 테스트로 실증하고,
> 틀리는 부분을 표준 발음법에 맞게 고쳐라. 단, 이 viseme 시스템은 음소를 10개
> 그룹으로 뭉치니, **입모양을 실제로 바꾸는 규칙만** 골라 과설계 없이 구현해라."

**진단(실측).** 테스트 스크립트로 현재 출력을 찍어 4가지 결함을 확인:
1. **겹받침이 전부 '중립(15)'으로 렌더** — `VISEME_MAP`에 ㄳ/ㄵ/ㄺ/ㅄ… 이 없어
   default 15. 예) "값[갑]"의 받침 입모양(양순)이 **사라짐**.
2. **무음 초성 ㅇ이 '연구개(7)'로 렌더** — 소리 없는 초성 ㅇ이 velar 입모양으로.
   예) "아이"가 [개방,전설]이 아니라 [연구개,개방,연구개,전설].
3. **연음 미적용** — `apply_phonological_rules`가 조합형 한글엔 안 걸리는 no-op였음.
   예) "밥을"이 [바블]이 아니라 철자대로(종성 ㅂ 닫고 + 초성 ㅇ velar).
4. **ㅎ탈락 미적용** — "좋아"가 [조아]가 아니라 성문(ㅎ) 유지.

**설계 판단(중요).** 표준 발음법 전체를 구현하지 않았다. 이 앱의 viseme는 10개
coarse 그룹이라 **비음화·경음화·격음화 등은 대개 같은 그룹 안에서 바뀌어
입모양이 변하지 않는다**(예: 국물→궁물의 ㄱ→ㅇ은 둘 다 '연구개'). 따라서
**입모양 그룹을 실제로 바꾸는 4가지 규칙만** 구현 — 겹받침 단순화, 연음(받침
이동, 겹받침은 뒤 자음만), ㅎ탈락, 무음 초성 ㅇ. (karpathy #2: 과설계 회피)

**구현(TDD).**
1. `backend/test_engine.py` 신규 — 11개 단어의 '정답 발음 음절'을 먼저 정의(성공 기준).
2. `to_pronounced_syllables(text)` 신규 — 텍스트를 '소리 나는 대로'의 음절
   [초성,중성,종성] 리스트로 변환(무음 초성은 ''). 인접 음절 규칙 적용.
3. `text_to_visemes`를 이 발음 변환 기반으로 재작성(무음 초성 프레임 생략).
4. 옛 no-op `apply_phonological_rules` 및 고아 `import re` 제거.

**검증(before → after).**
| 단어 | before | after |
|---|---|---|
| 값[갑] | 연구개·개방·**중립** | 연구개·개방·**양순** ✓ |
| 앉[안] | **연구개**·개방·**중립** | 개방·**치경** ✓ |
| 밥을[바블] | …종성ㅂ+**초성ㅇ velar**… | 양순·개방·양순·중설·치경 ✓ |
| 좋아[조아] | …**성문(ㅎ)**·연구개… | 경구개·원순·개방 ✓ |
| 아이 | **연구개**·개방·**연구개**·전설 | 개방·전설 ✓ |

- `python3 test_engine.py` → **11 passed, 0 failed**
- `import engine` 정상, 타 모듈 참조 영향 없음 확인.

**변경 파일.** `backend/engine.py`(발음 변환 신규 + text_to_visemes 재작성),
`backend/test_engine.py`(신규 — 기존에 없던 테스트 커버리지도 보강).

---

### [2026-07-09] 작업 #5 — 3D 모델 경량화 (35.1MB → 6.3MB)

**배경/목표.** 3D 입모양 모델(`realistic_face.glb`)을 정밀 진단한 결과 심각한
비효율 발견. 타겟(청각장애 학생 · 저사양 기기 · 불안정 통신망)에게 **첫 로딩
35MB는 치명적**이다. 입모양 학습에 필요한 것만 남겨 대폭 경량화한다.

**진단(모델 구조 해부).**
- **입 하나 보여주려고 전신 여성 모델을 로드.** 76개 노드 중 55개가 다리·발·
  손가락·척추·엉덩이. 카메라는 입만 클로즈업.
- 8개 메시 중 입모양에 필요한 건 **얼굴(base, 42개 입 모프)·이(teeth)·혀
  (tongue)** 뿐. 나머지는 **정장옷·포니테일·눈썹·속눈썹** — 전부 무관.
- 텍스처 18.5MB(전체 53%) 중 **옷 텍스처 8.4MB + 포니테일 3.7MB = 12MB**가
  화면에 보이지도 않는 것들. 압축(Draco/meshopt) 전혀 없음.
- (부가) 라이선스·출처 불명 — 대회 산출물 리스크. 향후 CC0 검증/교체 필요.

**AI에게 내린 지시(요지).**
> "GLB에서 입모양(얼굴·이·혀)에 필요한 것만 남기고 옷·머리·눈썹 등 무관한
> 메시와 텍스처를 제거해라. 단 얼굴의 입 모프타깃은 반드시 보존하고, 최적화
> 후 입모양이 정상 동작하는지 브라우저에서 육안 검증해라."

**실행.**
- `@gltf-transform`로 스크립트 작성: 무관 메시 4종(옷·포니테일·눈썹·속눈썹)
  분리·폐기 → `prune`(고아 텍스처 3개 자동 정리)·`dedup` → 남은 텍스처
  webp 압축 + 1024 축소. (원본은 백업.)
- 남은 메시: base(입 모프 보유)·high-poly·teeth·tongue. 텍스처 7→4개.

**검증.**
- **35.1MB → 6.3MB (82% 감소).**
- 최적화 모델을 앱에 교체 후 브라우저 육안 재검증: 양순(1) 입술 정상 다물림,
  원순(4) 입술 오므림 + 이·혀 정상 → **입모양 품질 100% 유지, 모프 손상 없음.**
- `npm run build` 성공.

**변경 파일.** `frontend/public/models/realistic_face.glb`(35.1MB→6.3MB 교체).
원본 백업 보관. (미사용 `avatar.vrm`은 별도 정리 대상.)

**남은 개선 여지(과설계 회피 위해 미실행).** base 메시의 미사용 모프타깃(66개 중
실사용 ~12개) 제거, meshopt 지오메트리 압축 → 추가 축소 가능하나, 6.3MB로 이미
충분하고 디코더 의존성·리스크가 붙어 현 단계에선 보류.

## 1. 3D 모델 모프타깃 감사(audit) — 매핑의 근거 확보

**배경/목표**
기존 `visemeShapes.js`의 viseme→블렌드셰이프 매핑은 "모델에 이런 셰이프가 있을
것"이라는 추정에 기대고 있었다. 정확도를 높이려면 먼저 `realistic_face.glb`가
**실제로 어떤 모프타깃을 가졌는지** 확정해야 한다.

**AI에게 내린 지시(프롬프트)**
> "realistic_face.glb를 파싱해서 각 메시의 morph target 이름을 전부 뽑아줘.
>  매핑에서 쓸 수 있는 ARKit / Oculus 블렌드셰이프가 뭐가 있는지 확인하자."

**실행 내용**
GLB(=glTF 바이너리)의 JSON 청크를 직접 파싱해 4개 메시의 targetNames를 추출.
- `base`(66개): ARKit 표준 셰이프 다수 + Oculus viseme_* 다수
- `teeth_base`(12개), `tongue01`(13개): jawOpen·viseme_* 공유
- `high-poly`(8개): 눈 관련
핵심 발견:
1. `mouthFunnel`, `mouthClose` 가 모델에 **존재하는데도** 기존 매핑에서
   전혀 쓰이지 않고 있었다(원순·양순 표현에 결정적인 셰이프).
2. `jawOpen` 은 base·teeth·tongue 3개 메시에 공통 존재 → 턱을 열면 치아·혀가
   함께 움직임. 반면 입술 셰이프(pucker/funnel/smile 등)는 base 전용.
   → "벌림=jawOpen, 입술모양=ARKit 립셰이프"로 역할을 나누는 근거 확보.

**검증**
파싱 결과 base 메시 66개 타깃 이름 전량 출력 확인.

**변경 파일**: (조사 단계, 코드 변경 없음)

---

## 2. viseme→블렌드셰이프 정밀 매핑 재작성

**배경/목표**
감사 결과를 바탕으로, 실제 존재가 확인된 ARKit 셰이프만 사용해 한국어 조음
위치(원순/개방/폐쇄/전설/치경)를 더 정확히 표현하도록 매핑을 재보정한다.

**AI에게 내린 지시(프롬프트)**
> "확인된 모프 키만 써서 visemeShapes.js를 다시 짜자. 특히
>  ① 양순음(ㅂㅍㅁ)은 mouthClose로 입술을 확실히 붙이고
>  ② 원순모음(ㅗㅜ)·이중모음은 mouthFunnel+mouthPucker로 앞으로 내민 둥근 O를 만들고
>  ③ 치경음(ㄷㄴㄹㅅ)에 잘못 들어가 있던 tongueOut(혀 내밀기)을 빼고 윗니가 보이게
>  바꿔줘. 조음음성학 근거를 주석으로 남기고."

**실행 내용 (before → after 요약)**

| viseme | before | after | 근거 |
|---|---|---|---|
| 1 양순음 | press+roll만 | **mouthClose 0.35** 추가 | 두 입술 확실히 폐쇄 |
| 4 원순모음 | pucker 0.95 단독 | **funnel 0.55 + pucker 0.6** + jaw 0.08 | 납작한 오므림 → 앞으로 내민 둥근 O |
| 9 이중모음 | pucker 0.28 | **funnel 0.28 + pucker 0.22** + jaw 0.2 | 원순+개방 혼합 표현 |
| 6 치경음 | jaw + **tongueOut 0.06** | jaw + **mouthUpperUp**(윗니 노출) | 조음상 혀끝은 잇몸 뒤 → 밖으로 안 나옴 |
| 2 개방모음 | jaw 0.42 | jaw 0.5 + mouthUpperUp | 개방 강화 |
| 3 전설모음 | smile+stretch | + **mouthUpperUp** | 좌우 확장 + 윗니 노출 |

**검증**
매핑이 사용하는 17개 키가 GLB `base` 메시에 **모두 존재**하는지 자동 대조:
```
keys used in mapping : 17
all present in GLB?  : True
missing (should be []): []
```

**변경 파일**: `frontend/src/lib/visemeShapes.js`

---

## 3. 죽은 코드·미사용 에셋 정리

**배경/목표**
기준선 진단(0.3)에서 지적된 죽은 코드/미사용 자산을 제거해 유지보수성과
빌드 산출물을 정리한다.

**AI에게 내린 지시(프롬프트)**
> "어디서도 import 안 되는 컴포넌트랑, 참조 없는 3D 에셋을 찾아서 지워줘.
>  단, 지우기 전에 정말 참조가 없는지 grep으로 확인하고."

**실행 내용**
참조 검색 후 제거:
- `frontend/src/components/Avatar3D.jsx` — 구(舊) 기하도형 얼굴. 어디서도 import 안 됨.
- `frontend/src/components/LipSyncPlayer.jsx` — 구 2D 플레이어. 현재 3D 플레이어로 대체됨.
- `frontend/public/models/avatar.vrm` (1.3MB) — 코드에서 참조 없음(AvatarVRM은 GLB 하드코딩).
추가로 `@pixiv/three-vrm` 의존성도 실제 import가 없어 제거 후보이나, 락파일
재생성 리스크를 피해 이번엔 문서화만 하고 남겨둠(후속 정리 대상).

**검증**
`grep -rn` 로 세 대상 모두 자기 정의 라인 외 참조 0건 확인. 이후 프로덕션 빌드
정상(아래 5절)으로 미사용 확정.

**변경 파일**: (삭제) Avatar3D.jsx, LipSyncPlayer.jsx, avatar.vrm

---

## 4. 2D 폴백 입모양 생성 + 3D 실패 시 자동 전환

**배경/목표**
기준선(0.3-2)의 약점: 2D 폴백 SVG가 "숫자 적힌 색깔 네모"라 3D 로드 실패 시
학습이 불가능했다. 실제 입모양 SVG로 교체하고, WebGL 미지원/GLB 로드 실패 시
자동으로 2D로 폴백하도록 연결한다.

**AI에게 내린 지시(프롬프트)**
> "숫자 네모 placeholder를 실제 입모양(개방/원순/폐쇄/전설/치경)을 그린 SVG로
>  바꾸는 생성 스크립트를 만들어 15장 뽑고, WebGL이 없거나 3D 모델 로드가 실패하면
>  이 2D 입모양으로 자동 폴백되게 AvatarVRM에 ErrorBoundary를 붙여줘."

**실행 내용**
- `generate_placeholders.py` 를 재작성 → 조음 특징(반폭·세로열림·원순도·치아·혀·
  폐쇄)을 파라미터로 하는 벡터 입모양 생성기. `1.svg~15.svg` 재생성.
- `MouthFallback2D.jsx` 신설: 현재 viseme의 입모양 SVG를 뷰포트에 표시.
- `AvatarVRM.jsx`: WebGL 지원 감지(`detectWebGL`) + `GLErrorBoundary` 추가.
  3D 컨텍스트 생성 실패나 GLB 로드 오류 시 `MouthFallback2D`로 전환.
  `<RealisticFace>`를 `<Suspense>`로 감싸 로드 오류가 경계로 전파되게 함.

**검증**
- 15개 SVG XML 파싱 정상.
- 렌더 컨택트시트로 15개 입모양이 시각적으로 구분됨을 육안 확인
  (폐쇄/개방/둥근 O/좌우확장/치아·혀 노출이 뚜렷).

**변경 파일**: `frontend/public/visemes/*.svg`(15),
`frontend/public/visemes/generate_placeholders.py`,
`frontend/src/components/MouthFallback2D.jsx`(신규),
`frontend/src/components/AvatarVRM.jsx`

---

## 5. 전체 검증(회귀 없음 확인)

**배경/목표**
변경 후 백엔드 발음 변환 로직과 프론트엔드 빌드가 깨지지 않았는지 확인.

**실행 내용 / 검증 결과**
- 백엔드 엔진 테스트: `python3 backend/test_engine.py` → **11 passed, 0 failed**
  (연음·겹받침·ㅎ탈락·무음 초성 ㅇ 규칙 정상).
- 프론트엔드 프로덕션 빌드: `npm run build` → **✓ built**, 1019 modules transformed,
  삭제한 파일/에셋으로 인한 오류 없음.
- 매핑 키 존재 대조: 17/17 GLB에 존재.

**변경 파일**: (검증 단계, 코드 변경 없음)

---

## 6. 브라우저 육안 검증 + 카메라 버그 수정 + 원순 입모양 미세조정

**배경/목표**
지금까지의 매핑은 "모프 키가 존재한다"는 정적 검증에 그쳤다. 독화 앱의 생명은
**실제 렌더링된 입모양이 조음에 맞고, 서로 명확히 구분되는가**이므로, 15종
viseme을 브라우저에서 실제로 렌더링해 눈으로 확인하고 어긋난 곳을 고친다.

**AI에게 내린 지시(프롬프트)**
> "viseme 1~15를 브라우저에서 실제로 렌더링해서 하나씩 스크린샷으로 확인하고,
>  3D 입모양이 조음(폐쇄/개방/원순/전설/치경)에 맞는지, 서로 구분되는지 검증해라.
>  틀린 곳이 있으면 실제 렌더 결과를 보면서 고쳐라."

**실행 내용**
1. **검사용 페이지 신설** — `/dev-viseme` 라우트(`pages/DevViseme.jsx`). `?v=N`으로
   특정 viseme을 지정해 **3D(좌) + 2D SVG 폴백(우)을 나란히** 렌더링하고 적용된
   블렌드셰이프 가중치를 표로 보여준다. 스크린샷 단위 회귀 검증이 가능해진다.
2. **15종 순차 스크린샷 검증.** 대부분(1 양순 폐쇄·3 전설 확장·6 치경 치아노출·
   7 연구개·8 성문·10 경구개·15 rest)은 조음에 맞고 서로 잘 구분됨을 확인.

**발견 ① — 카메라 경쟁 조건(버그, 수정).**
같은 viseme인데도 **어떤 로드에서는 입이 아니라 눈이 잡히는** 현상 발견(재현됨).
원인: `AvatarVRM`이 `<Canvas>`의 자식으로 `<PerspectiveCamera makeDefault>`를 두어,
`<OrbitControls>`가 카메라의 입 클로즈업 위치(`[0,1.62,0.45]`, 반경 0.45)가 설정되기
**전에** three의 기본 위치 `[0,0,5]`(반경 5.25)를 읽는 순간이 생긴다. 이때 OrbitControls가
`target` 기준 반경 5.25에 `maxPolarAngle(100°)`로 카메라를 고정 → 얼굴 전체(눈)가 잡힘.
React StrictMode(개발)의 이중 마운트가 이를 자주 재현시킴. **실제 앱의 `LipSyncPlayer3D`도
같은 `AvatarVRM`을 쓰므로, 사용자가 간헐적으로 입 대신 눈을 보게 되는 실사용 버그였다.**
- **수정:** 카메라를 `<Canvas camera={{ position:[0,1.62,0.45], fov:16 }}>`로 직접 지정
  (렌더러 생성 시점에 확정) → 별도 `<PerspectiveCamera>` 제거. OrbitControls가 항상
  올바른 위치를 읽어 입 클로즈업이 안정적으로 고정됨(연속 리로드 재검증 통과).

**발견 ② — 원순/이중모음 입모양 미세조정.**
`realistic_face.glb`는 **jawOpen이 조금만 커져도 윗니가 드러나** 원순의 둥근 특성을
해친다(모프 품질 한계). 반대로 funnel+pucker를 높이면 입술이 닫혀 개구부가 사라진다.
"둥근 입술 + 둥근 구멍 + 치아 감춤"을 동시에 못 내는 모델이라, **치아 노출을 피하면서
앞으로 내민 원순 protrusion을 최대 단서로** 삼도록 조정.

| viseme | before | after | 근거(육안) |
|---|---|---|---|
| 4 원순 ㅗㅜ | funnel .55 / pucker .60 / **jaw .08** | **funnel .62 / pucker .48 / jaw .05** | jaw↑는 치아 노출로 개방모음처럼 보임 → jaw 최소·funnel 강화로 '앞으로 내민 둥근 입술'을 살림 |
| 9 이중모음 | funnel .28 / pucker .22 / jaw .20 | **funnel .38 / pucker .32 / jaw .16** | v4(꽉 둥근)와 v2(활짝 개방)의 중간. 치아 과다 노출 없이 '둥글게 살짝 벌림' |

**핵심 인사이트(문서화 가치).** "정적 키 검증(모프가 존재하는가)과 실제 렌더 검증
(입모양이 맞게 보이는가)은 다르다." 카메라 버그는 정적 분석으로는 절대 안 잡혔고,
원순 입모양의 모델 한계도 스크린샷을 눈으로 봐야만 드러났다. **육안 검증 루프가
실사용 버그 1건과 부정확 입모양 2건을 걷어냈다.**

**검증**
- `/dev-viseme`로 카메라 안정성 재현 테스트(연속 리로드 → 항상 입 고정).
- 매핑 키 존재 대조: **17/17 GLB에 존재**(회귀 없음).
- 백엔드 엔진 테스트 **11 passed**, 프론트 프로덕션 빌드 **✓ built**.

**변경 파일**: `frontend/src/components/AvatarVRM.jsx`(카메라 버그 수정),
`frontend/src/lib/visemeShapes.js`(원순·이중모음 재보정),
`frontend/src/pages/DevViseme.jsx`(신규, 검사 도구), `frontend/src/App.jsx`(라우트).

---

## 7. 신규 모듈 — 한국어→한국수어(KSL) 학습 보조

**배경/의사결정.** "농인에게 한국어는 제2언어이고 제1언어는 수어"라는 문제의식에서
출발. 근거 조사([수요 인용문헌](docs/sign-translation-references.md)): 수어 사용 농인의
**90.8%가 "가장 적절한 언어는 한국수어"**(국립국어원 2023), 중증 청각장애인 **30.1%가
수어 주 사용**, 청각장애 학생의 한국어 문해 취약. 단, "범용 웹 번역기/브라우저 확장"은
**범위에서 제외**했다 — 이미 이큐포올·KLcube 등이 선점했고, 개인 임의-웹 수요 미입증 +
농인 검토자 부재로 품질·윤리 리스크가 크다(자세한 논의는 설계문서). 대신 **LIPLAB 내부
학습 보조 모듈**로 한정하고, *통역*이 아닌 **"학습·이해 보조(베타)"**로 정직하게 포지셔닝.

**AI에게 내린 지시(요지).**
> "정해진 값이 아니라 실제로 번역이 되게, 한글→한국수어 모듈을 LIPLAB 안에 넣어라.
>  하드코딩 금지. 실제 수어 데이터를 쓰고, 우리 입모양 엔진과 연결해라."

**설계 — 2단계(둘 다 실제 처리).**
- **Stage A(언어 번역):** Claude가 한국어를 **KSL gloss**로 번역(조사 제거·어순 재배열·
  비수지 표지 주석·기본형 표제어 산출). API 키가 없으면 **규칙기반 폴백**(조사 제거)으로
  동작하며, 응답에 `method: "llm"|"rule"`을 실어 어느 경로인지 정직하게 표시.
- **Stage B(시각화):** gloss 단어를 **국립국어원 한국수어사전 「한국어대응표현정보」
  (13,950 표제어)**에서 조회 → 수형설명 + 표제어번호(`origin_no`)로 **실제 수어 영상 딥링크**.
  사전에 없는 단어는 **지문자(지화)** 로 자모 분해(엔진의 `decompose_hangul` 재사용).
- **시너지(차별점):** 각 수어 단어의 **입모양(mouthing)** 을 기존 viseme 엔진
  (`text_to_visemes`)으로 함께 제공 → **"수어 + 입모양"을 한 화면에**. 키오스크 업체도
  웹 위젯도 안 하는 조합이자, 독화 모듈과 수어 모듈이 기술적으로 연결되는 지점.

**구현.**
- 백엔드 `sign_service.py`(사전 인덱스·gloss 번역·지문자·조립), `POST /api/sign/translate`,
  데이터 `backend/data/ksl_dictionary.csv`(공공누리, 출처표시).
- 프론트 `pages/Sign.jsx`(입력→토큰 시퀀스: 수어 카드=수형설명+국립국어원 영상 딥링크,
  지문자 카드=자모 / 단일 아바타로 토큰별 입모양 재생), 라우트 `/sign`, 대시보드 "수어" 메뉴.

**검증(회귀 없음 + end-to-end).**
- `python3 backend/test_sign_service.py` → **7 passed**(조회·지문자·규칙 폴백·조립).
- 기존 엔진 테스트 **11 passed**, 프론트 빌드 **✓ built**.
- **브라우저 실제 동작 확인**: sqlite 백엔드 기동 → 로그인 → `/sign`에서 "학교에 갔어요"
  → "학교"는 실제 수어(origin_no 1352·수형설명·영상 딥링크), "갔어요"는 지문자 폴백,
  "전체 재생" 시 각 단어의 입모양을 아바타가 순차 렌더(카메라 수정 유지). 스크린샷 확보.

**LLM 경로 실검증(ANTHROPIC_API_KEY 설정).** 실제 Claude 번역 경로를 예문으로 확인:
| 입력 | 결과(method=llm) |
|---|---|
| 학교에 갔어요 | "갔어요"→**가다** 원형화·매칭, 조사 '에' 제거, "시제표지 없이 문맥 처리" 메모, 평서문 비수지 |
| 천천히 말해 주세요 | "말해"→**말하다** 매칭, **"주세요"를 비수지(요청: 눈썹 올리기+부드러운 표정)**로 처리 |
| 밥 먹었어요? | "먹었어요"→**먹다** 매칭, **의문 비수지(눈썹 올리기+몸 기울이기)** = KSL yes/no 의문문 |
→ 규칙 폴백이 못 하던 **용언 원형화·조사 제거·시제/의문/공손의 비수지 표지 처리**를 Claude가
실제로 수행함(브라우저에서 "AI 번역(Claude)" 배지로도 확인). 헤드라인 주장(실제 번역) 입증.

**정직성·한계(문서화).** 그럼에도 고립 사전영상/지문자 조합은 유창한 KSL이 아닌 **근사**이며
UI에 "베타·학습 보조" 라벨 상시 표기, 통역 대체 주장 안 함. 사전 미등재 부사·고유명사
("천천히"·"밥" 등)는 지문자로 빠진다(향후 사전 커버리지·동의어 보강 여지).

**변경 파일**: `backend/sign_service.py`·`backend/test_sign_service.py`(신규),
`backend/main.py`(엔드포인트), `backend/data/ksl_dictionary.csv`(신규 데이터),
`frontend/src/pages/Sign.jsx`(신규), `frontend/src/App.jsx`·`frontend/src/pages/Dashboard.jsx`·
`frontend/src/api.js`(연동), `docs/sign-translation-design.md`·`docs/sign-translation-references.md`(신규).

---

## 8. 마감 다듬기 — 경량화·통합·라이선스·커버리지

**배경/방법.** 두 축(독화·수어)이 기능적으로 완성된 뒤, 배포 전 마감을 정리했다.
여러 독립 항목이라 **조사(감사·설계·리서치)를 병렬 서브에이전트로 팬아웃**해 근거를
확보하고 본 스레드에서 구현·검증했다.

**8.1 3D 경량화 — 지연로딩 + 죽은 의존성 제거.** (⚠️ 적대적 리뷰로 1차 시도의 오류를 잡음)
- 진단: three(@react-three) 벤더 청크(~320KB gzip)가 **로그인/대시보드 초기 진입에서도**
  로드되고 있었다(three는 오직 3D 페이지만 씀). 저사양·불안정 통신망 타겟에 불리.
- 조치: three를 쓰는 4개 페이지(Practice·**Conversation**·Sign·DevViseme)를 `React.lazy`+
  `<Suspense>`로 지연로딩. **숨은 3D 페이지 Conversation을 빠뜨리면 최적화가 무효**가 되는
  점을 조사에서 잡아 포함.
- **시행착오(중요).** 1차엔 `three-vendor` 수동청크를 유지하고 `modulePreload.resolveDependencies`로
  preload 힌트만 제거했는데, **적대적 리뷰가 이게 무효임을 실측으로 지적**: 수동청크로 두면
  Rollup이 공유 헬퍼(`__vitePreload`)·zustand를 무거운 three 청크로 **호이스팅**해, 엔트리
  `index.js`가 three-vendor를 **정적 import** → preload 힌트를 지워도 초기 로드에서 three가
  그대로 받아졌다(오히려 늦게 발견돼 waterfall). → **수정: three를 수동청크로 지정하지 않고**
  Rollup 자동 async 분리에 맡겨(three는 lazy 페이지만 참조하는 async 청크로 남음), react/framer만
  벤더 분리. **정적 import 그래프 추적 스크립트로 "첫 페인트에 three 부재"를 결정론적으로 검증.**
- 미사용 `@pixiv/three-vrm` 의존성 제거(소스 어디서도 import 0건; 아바타는 GLB/GLTF 기반).
- **결과(실측): 첫 페인트 정적 로드 385KB → 130KB gzip**(three ~256KB gzip이 초기에서 빠짐),
  three는 3D 페이지 진입 시에만 로드. 청크 로드 실패(불안정망)는 상위 ErrorBoundary가 잡아
  **사용자 친화 "새로고침" 화면**을 보이도록 개선(원본 스택 노출 제거, 개발모드만 상세 표시).

**8.2 학습 흐름 통합.** Sign 페이지가 `/sign?text=문장` 쿼리로 자동 번역하도록 하고,
Practice(학습 모드)에 **"이 문장 수어로 보기"** 버튼을 추가해 현재 학습 문장을 그대로
수어로 연결. 독화 연습과 수어 이해가 한 흐름으로 이어진다.

**8.3 라이선스 정정·출처표시(중요).** 조사에서 **기존 통념을 정정**: 국립국어원 한국수어사전
동영상은 공공누리 제1유형이 아니라 **CC BY-NC-ND 2.0 KR**(저작자표시·비영리·변경금지)이며
일부 영상은 국립국어원 비소유. → **영상은 임베드하지 않고 표제어 딥링크만** 유지하는 것이
안전(ND·NC·비소유 리스크 회피)함을 확정. 텍스트 데이터셋(15135637)은 "이용허락범위 제한
없음". 수어 페이지 하단에 **출처표시 문구를 상시 노출**하고 관련 문서를 정정.

**8.4 사전 조회 = 완전일치만(오탐 제거).** (⚠️ 적대적 리뷰로 방향 전환)
처음엔 규칙기반 정규화(조사 제거·용언 원형화)를 index-gate로 넣었으나, **리뷰가 실측 반례로
치명적 오탐을 지적**: 자모 복원 없는 절단이 ㅅ/ㄷ 불규칙·명사를 *사전에 실재하는 다른 표제어*로
오매칭한다 — 지었다→**지다**(lose), 물었다→**물다**(bite), 나았다→**나다**, 정의→**정**. index-gate는
'존재 여부'만 봐서 못 막고, 학습자에게 **틀린 수어 영상·수형·입모양을 확신 있게** 제시한다
(미매칭보다 나쁨). → **정규화를 전면 철회하고 `lookup_sign`을 완전일치 전용으로 되돌렸다.**
근거: **기본형 변환은 주 경로인 LLM(Claude가 기본형 표제어 출력)이 이미 수행**하므로 정규화의
이득은 오프라인 폴백뿐인데, 오탐이 LLM 경로의 명사까지 오염시켰다. 완전일치 실패는 정직하게
지문자로 폴백. 오탐 방지 회귀 테스트 추가(지었다·물었다·정의 → 오매칭 안 됨을 assert).

**검증.** 백엔드 엔진 **11 passed** + 수어 서비스 **10 passed**(완전일치·오탐 방지·지문자 폴백),
프론트 빌드 **✓ built**, **첫 페인트 정적 그래프에 three 부재를 스크립트로 검증**, 브라우저 e2e
(쿼리 자동번역·출처표시 노출·lazy 3D 렌더·LLM 번역) 확인. **이번 마감은 적대적 리뷰(3차원
finder→검증)가 실사용 결함 5건(three 초기로드·수어 오매칭 2건·청크 에러 노출·길이 우회)을
커밋 전에 걷어낸 것이 핵심 — 정적 분석/테스트만으로는 안 잡히는 것을 실측 재현으로 잡았다.**

**변경 파일**: `frontend/src/App.jsx`·`vite.config.js`·`frontend/package.json`(경량화·에러UI),
`frontend/src/pages/Sign.jsx`·`frontend/src/pages/Practice.jsx`(통합·출처표시·길이가드),
`backend/sign_service.py`·`backend/test_sign_service.py`(완전일치 회귀), `backend/main.py`(엔드포인트),
`docs/sign-translation-references.md`·`docs/sign-translation-design.md`(라이선스 정정).

---

## 9. 데모 편의 — 로그인 제거, 게스트 자동 입장

**배경.** 공모전 심사·시연 시 회원가입/로그인 장벽 없이 바로 앱을 체험하게 하는 것이
깔끔하다. 인증 체계를 통째로 걷어내면 모든 엔드포인트(진행도·북마크·분석)의
`get_current_user` 의존성을 손대야 해 리스크가 크므로, **인증은 유지하되 부팅 시 게스트
계정으로 자동 로그인**하는 방식을 택했다(진행도·북마크 등 기존 기능 그대로 동작).

**구현.** 백엔드 `POST /api/auth/demo`(멱등: 게스트 계정 없으면 생성 후 토큰 발급,
동시요청 경쟁은 롤백·재조회로 처리). 프론트 `AuthGate`가 미인증 시 이 엔드포인트로 자동
로그인하고 완료까지 스플래시를 보인다. `/login` 라우트·`Login` 페이지·`ProtectedRoute`를
제거하고 루트를 대시보드로 직행, 로그아웃 버튼 제거. 토큰 만료(401) 시엔 재부팅으로
`AuthGate`가 조용히 재로그인(무한루프 방지 가드 포함).

**검증.** localStorage를 비운 신규 진입(`/`)에서 로그인 화면 없이 게스트로 대시보드 입장,
학습·수어 등 전 기능 정상 확인.

---

## 10. 수어 UX 재설계 — 실제 영상 인라인 + 학습흐름 통합

**문제(사용자 피드백).** "수어로 보기를 눌렀는데 왜 입모양만 뜨나?", "별도 탭으로
왔다갔다 하라는 거냐?" — 정당한 지적. 기존 설계는 (1) 실제 수어는 *텍스트 수형설명 +
외부 딥링크*로만 있고 화면을 지배하는 건 입모양 아바타였고(수어 번역기인데 수어가 안 보임),
(2) 수어가 별도 `/sign` 페이지 + 국립국어원 외부 탭으로 흩어져 있었다.

**원인·해결의 실측 조사.** 처음엔 라이선스(CC BY-NC-ND) 때문에 영상을 임베드하지 않고
딥링크만 했는데, 다시 조사하니 **인라인 임베드가 가능**했다:
- 영상이 **HTTPS로도 재생**됨(`https://sldict.korean.go.kr/multimedia/.../MOV..._700X466.mp4`,
  200 video/mp4) → HTTPS 앱에서 mixed-content 없음.
- 표제어 상세 raw HTML의 **썸네일 경로**(`.../MOV{num}_105X105.jpg`)로 **origin_no → 영상 URL을
  유도** 가능(공식 API 키·JS 스크레이핑 불필요). robots.txt의 `/front/sign` 차단은 **Googlebot 전용**.
- 비영리·무변형·출처표시 조건에서 임베드는 CC BY-NC-ND에 부합(상업화 시 별도 허락 필요).

**구현.**
- 백엔드 `resolve_video_url(origin_no)`: 상세 페이지를 1회 조회해 썸네일 경로에서 mp4 URL을
  유도·캐시(스레드 오프로딩·병렬 gather). `translate_to_ksl`의 수어 토큰에 `video_url` 부착.
- 프론트 `components/SignPanel.jsx`(신규, 공용): **실제 수어 영상을 메인 패널에 인라인 재생**
  (자동재생·무음·루프, 전체재생 시 `onended`로 다음 토큰 진행). 입모양은 **"입모양 함께"
  토글(기본 off)** 로 강등. 지문자 토큰은 자모를 크게 표시. 딥링크는 작은 '출처' 링크로.
- `Sign.jsx`는 입력·예시만 두고 SignPanel에 위임. **Practice에는 별도 페이지 이동 대신
  슬라이드오버 모달**로 SignPanel을 띄워(학습흐름 이탈 없음) 탭 왕복을 제거.

**검증.** 백엔드 리졸버가 실제 mp4로 해석(학교→MOV000236222…), 엔진 11·수어 10 테스트
통과(영상 리졸버는 테스트에서 스텁), 프론트 빌드 ✓·three 지연 유지. 브라우저에서 `/sign`이
**실제 국립국어원 수어자 영상을 메인으로 인라인 재생**함을 확인(학교·가다 등). Practice
모달은 동일 SignPanel을 재사용.

**추가 — 지문자를 실제 손모양 이미지로.** "지문자로 뜨는 것들이 자모 글자만 보인다"는
지적에, 손모양 이미지 소스를 다시 찾아 **Wikimedia Commons "Korean manual alphabet"
(© Kwamikagami, CC BY-SA 3.0)** 32자를 확보(자모↔로마자 파일명, 라이선스·URL을 API로
확정). `frontend/public/fingerspell/`에 로컬 서빙, `lib/fingerspell.js`의 자모→이미지 매핑으로
SignPanel의 지문자 토큰을 **자모별 손모양 사진**으로 렌더(미커버 된소리·w복합모음은 글자
폴백). 출처표시 문구에 CC BY-SA 3.0 추가. 브라우저에서 밥→ㅂ·ㅏ·ㅂ 손모양 표시 확인.

**추가 — 영문(라틴) 지문자.** "영어는 커버가 안 된다"는 지적에, 한국수어엔 라틴 문자 지문자
표준이 없어 **국제(미국식) 지문자**를 차용하기로 하고(Wikimedia Commons "Sign language A~Z",
26자 **Public Domain** SVG), `public/fingerspell/latin/`에 서빙. `fingerspell.js`가 라틴 문자를
`/fingerspell/latin/{대문자}.svg`로 매핑. LLM이 외래어("AI")를 지문자로 판단 → A·I 손모양으로
표시됨을 브라우저에서 확인. → 사전 있는 한국어=실제 영상, 없는 한국어=한글 손모양,
**영어=국제 지문자 손모양**으로 **거의 모든 입력이 시각 표현**된다(된소리·w복합모음·숫자만 폴백).

---

## 11. 수어 커버리지 — 별칭표(투명 치환) + 숫자 수어

**11.1 별칭표(수기 검증 + 투명 치환).** "지문자로 빠지는 흔한 단어를 수어로"라는 요청에,
사전을 실측해보니 **대부분의 흔한 명사는 이미 수어로 존재**(핸드폰·지하철·화장실·컴퓨터·
버스…)했고 진짜 OOV는 소수였다. 그 소수를 **사람이 검증한 근접 동의어**로 연결하는 소규모
별칭표(`_ALIASES`, 11개: 밥→식사, 스마트폰→휴대폰, 선생님→교사, 자동차→차, 사용하다→이용
등)를 backend에 두고, **사전 존재를 자동 검증**(테스트). 지난 리뷰가 위험하다고 한 알고리즘
절단과 달리 **명시적·감사 가능**하다. UI에는 치환을 **투명하게 표시**("밥 → 식사", 하단에
"'밥'은 사전에 없어 근접 수어 '식사'로 표시합니다") — 틀린 수어를 숨기지 않는다. 너무 느슨한
후보(마트→시장 등)는 의도적으로 제외.

**11.2 숫자 → 실제 KSL 숫자 수어.** 지숫자(손모양) 이미지는 CC 세트가 없고, 무엇보다 사전에
**숫자마다 실제 수어 영상**이 있어(하나~열 등) 그쪽이 더 정확. 아라비아 숫자를 **자리별로
고유어 수사(하나~아홉)의 수어 영상**으로 변환한다. 고유어 수사는 표제어가 '손가락 폄' 수형이
명확하고 동형어가 적어 안전(한자어 일·이·사는 동형어 다수 → 오매칭 위험이라 미사용). LLM
프롬프트에 "숫자는 별도 토큰으로 분리"를 추가해 "3개"→"3"+"개"로 쪼개지게 함. 치환은 별칭과
같은 투명 표시("3 → 셋").

**0 처리(수정).** 처음엔 0을 지문자 폴백했으나, '영'의 entries[0]이 천주교 동형어라 못 잡았을
뿐 실제로는 **숫자 0 수어(origin 1214, '개념>수' 카테고리, O 모양 손)**가 있었다. 동형어 중
**'수' 카테고리를 우선 선택하는 `lookup_number_sign`**을 추가해 0→영(1214)을 정확히 잡는다.
→ "2024년"이 2·0·2·4·년 **전부 수어 영상**(지문자 0)으로 나온다.

**검증.** 수어 서비스 **13 passed**(별칭 값 사전존재·치환·숫자 포함), 빌드 ✓·three 지연 유지.
브라우저 "밥 3개 먹었어요" → 밥(→식사)·3(→셋)·개·먹다 **전부 수어 영상**(지문자 0), 투명
치환 문구 표시 확인. **변경**: `backend/sign_service.py`·`test_sign_service.py`,
`frontend/src/components/SignPanel.jsx`(signed_as 투명 표시).

---

## 12. 전역 텍스트 선택 → 수어 번역 (앱 어디서나)

**요청.** "수어 탭으로 가야만 번역되는데, 웹사이트 **어느 문장이든** 원하면 바로 번역해서
보고 싶다." → 앱 전역에 **텍스트 선택 → 그 자리에서 수어**를 붙인다.

**구현.** `components/SignSelectionOverlay.jsx`(신규)를 App 루트(AuthGate 내부)에 1회 마운트.
문서 `mouseup`을 듣다가 한국어가 포함된 2~200자 선택이 감지되면 선택 위치 근처에 **플로팅
"🤟 수어로 보기" 버튼**을 띄우고, 누르면 슬라이드오버로 **기존 SignPanel**을 그 문장으로 연다
(페이지 이동 없음). 어느 화면(대시보드·학습·가이드…)에서든 동작.

**경량화 주의(회귀 방지).** 전역 오버레이가 SignPanel(→three)을 정적 import하면 초기 번들에
three가 다시 실린다. 그래서 (1) 오버레이는 SignPanel을 **지연로딩**(모달 열 때만), (2) SignPanel
내부의 입모양 `LipSyncPlayer3D`(three)도 **"입모양 함께" 토글 시에만 지연로딩**하도록 분리.
→ 수어 영상만 볼 땐 three를 아예 안 받는다.

**검증.** 빌드 ✓, **첫 페인트 그래프에 three 부재 유지**(SignPanel 7KB 별도 청크). 브라우저에서
`/guide` 문단 선택 → 버튼 → 모달로 복잡한 문장(수어 31 + 지문자 8)이 번역되고 "LIPLAB"이
영문 손모양(L·I·P·L·A·B), 숫자 10이 1+0으로 분리됨을 확인. **변경**:
`frontend/src/components/SignSelectionOverlay.jsx`(신규), `SignPanel.jsx`(입모양 지연), `App.jsx`(마운트).

---

> 요약: 이번 고도화는 (1) 모델 모프타깃을 **실측 감사**해 근거를 확보하고,
> (2) 그 위에서 한국어 조음에 맞는 **정밀 입모양 매핑**으로 재보정했으며,
> (3) 죽은 코드/에셋을 정리하고, (4) 3D 실패 상황에서도 학습이 끊기지 않도록
> **의미 있는 2D 폴백**을 붙였고, (5) **브라우저 육안 검증 루프**로
> 실사용 카메라 버그 1건을 잡고 원순 입모양 2종을 실제 렌더 기준으로 재보정했으며,
> (6) 농인의 제1언어 접근성을 위해 **한국어→한국수어 학습 보조 모듈**을 신설했으며
> (Claude gloss 번역 + 국립국어원 실제 수어 데이터 + 입모양 시너지, "베타·학습 보조"로 정직 포지셔닝),
> (7) 끝으로 **마감 다듬기**로 3D 지연로딩(초기 로드에서 three ~320KB 제외)·죽은 의존성 제거·
> 학습 흐름 통합·라이선스 정정 및 출처표시·사전 커버리지(오탐 방지 index-gated 정규화)를 정리했다.
> 모든 변경은 엔진·수어 서비스 테스트, 프로덕션 빌드, 브라우저 end-to-end로 검증했다.

---

# 제2기 고도화 — 「LIPLAB 고도화계획서」 축 A~K 실행

> 위까지가 이전 대회 제출본을 다듬은 1기 기록이고, 여기부터는 2026-08-25 작성한
> **「멀티모달 발음·독화 평가를 중심으로 한 LIPLAB 고도화」 계획서**(고도화 축 A~K)를
> 실제 코드로 구현하는 2기 기록이다.
>
> **실행 순서(합의).** 데이터·인프라 의존이 낮은 것부터 착수한다.
> Phase 0 (로컬 즉시): **G 콘텐츠 대량화 → J 시각 증강 → D 웹캠 입모양 채점**.
> Phase 1 (백엔드 핵심): B 전사 비의존 D-GOP · C 지각공간.
> Phase 2 (백본): A 공유 백본+농인 발화 합성 · E 조음 진단.
> Phase 3 (시연·통합): F 아바타 · K 확장 단서 · H 다자 시나리오 · I 표준검사.

## G. 콘텐츠 대량화 및 개인화 (Content Scaling)

### G.0 배경/목표
계획서 §3.7의 진단: 현행 커리큘럼은 기초 단계 콘텐츠 분량이 얕다. 실제로 코드를
열어 보니 `backend/curriculum.py`에 **단어 은행 24개, 최소대립쌍 10개, 문맥추론
문항 5개**가 파이썬 상수로 하드코딩되어 있고, `/api/curriculum/words|closure`가 이를
그대로 반환한다. 학습 경로를 채우기엔 부족하다.

목표는 이 콘텐츠를 **LLM 대량 생성 + 규칙 검사 + (후속)사람 검수**의 이중 게이트로
수백~수천 규모로 확장하는 것이다. 핵심 설계 판단은 **"생성은 LLM, 관계는 규칙"**이다.
LLM에게 '같아 보이는 단어쌍'을 직접 물으면 음운 환각이 크므로, LLM은 단어만 잘 내게 하고
동구형이음·최소대립 같은 음운 관계는 기존 비심(viseme) 엔진으로 결정론적으로 계산한다.

### G.1 규칙 검사 게이트 (`backend/content_rules.py` 신규)
**지시(사용자 프롬프트).** "LIPLAB 이해 → 고도화계획서(축 A~K)를 기준으로 → 로컬에서
가능한 것부터 → 전체 실행 순서 추천 → 로컬에서 가능한 것부터 시작." 이에 따라 Phase 0의
첫 항목인 축 G에 착수했다.

**실행.** `engine.py`의 `decompose_hangul`·`VISEME_MAP`(1~10 그룹)·`to_pronounced_syllables`를
재사용해 순수 함수 게이트를 구현했다. 채점·애니메이션과 **동일한 음운 근거**로 콘텐츠를
검사하는 것이 요점이다.
- `word_visemes`/`viseme_signature`: 단어를 '소리 나는 대로' 변환한 뒤 실제로 보이는
  입모양 순열만 추출. 두 단어의 시그니처가 같으면 눈으로 구별 불가(동구형이음).
- `check_word`: 한글 유효성·음절수·비심 유효성 검사, tier 자동 부여.
- `check_lookalike_pair`: 교육쌍을 **동구형이음(시그니처 동일)** 또는 **최소대립(자모 한
  자리 차이)**으로 판정하고, `same_looking`을 규칙으로 재계산해 LLM 라벨을 교정한다.
- `check_closure`: 문맥추론 문항이 성립하는지(빈칸·정답·**시각적으로 혼동되는 오답 필수**) 검사.
- `discover_pairs`: 단어 풀에서 시그니처 버킷·자모 와일드카드 버킷으로 교육쌍을 자동 발굴.

**검증(결정론적, 기존 데이터로).**
- `WORD_BANK` 24/24, `CLOSURE_ITEMS` 5/5 통과. 반례(영문·4음절·비최소대립·혼동없는 보기) 정확히 차단.
- 기존 `MINIMAL_PAIRS` 10건 중 8건 통과, `same_looking` 규칙값이 수동 라벨과 **100% 일치**.
  탈락 2건(**바다/파도·밥/입**)은 자모 두 자리가 달라 엄밀한 교육쌍이 아님 → 게이트가
  기존 데이터의 느슨함을 잡아낸 사례.
- **초기 발견·수정.** 첫 게이트는 최소대립쌍만 인정해 '밥/맘'을 탈락시켰으나, 밥/맘은
  시그니처가 `[1,2,1]`로 완전히 같은 **동구형이음**(더 강력한 교육 사례)이었다. 게이트를
  '동구형이음 또는 최소대립'을 모두 받도록 확장했다.
- **발굴 실증.** `WORD_BANK` 24단어 → **22쌍 자동 발굴**(동구형이음 10 + 최소대립 12).
  기존 수동 10쌍 대비. 단어를 수백으로 늘리면 쌍은 조합적으로 증가한다.

### G.2 생성 파이프라인 (`backend/content_pipeline.py` 신규)
**실행.** "생성은 LLM, 관계는 규칙" 원칙으로 구성했다.
- `build_word_bank`: `content_gen.generate_words`(Claude)를 반복 호출 → `check_word` 게이트 → 목표 개수.
- `discover_pairs`: 규칙으로 쌍 발굴(LLM 미사용).
- `build_closures`: 혼동쌍(same_looking=True)마다 Claude에게 "정답이 유일하게 자연스럽고
  오답을 넣으면 어색한 문맥 문장"을 생성시키고 `check_closure`로 재검증.
- `generate_all`: 위를 묶어 status="candidate"로 반환(사람 검수 전).

**변경 파일(G.1~G.2).** `backend/content_rules.py`(신규), `backend/content_pipeline.py`(신규).
브랜치 `feat/content-scale`(base: `integration`).

### G.3 실행 환경 이슈와 해결 (iCloud eviction)
작업본이 iCloud Drive 경로(`.../k-AI/liplab`)에 있어 `.venv` 패키지가 클라우드에서
evict되었고, `import anthropic` 하나가 40초를 넘겼다. iCloud 경로 파이썬 실행은 부적합.
→ **실행만 로컬 디스크(`~/Downloads/liplab`)에서** 하도록 분리했다(코드는 iCloud 작업본 유지).
로컬 venv에 최소 의존만 설치하니 import 3초로 정상화. 다만 로컬에 최신 **anthropic 1.3.0**이
깔려 `messages.create()`의 `temperature` 인자가 사라진 API 변경으로 실패 → 프로덕션과
동일한 **0.116.0으로 고정**해 코드 무수정으로 맞췄다(로컬은 검증용, 프로덕션 SDK와 일치가 원칙).

### G.4 쌍 수율 문제와 동구형이음 파트너 확장 (핵심)
첫 실측에서 문제를 발견했다. 무작위 생성 단어 25개로는 **발굴 쌍이 0개**였다. 서로 다른
범주의 임의 어휘는 입모양 시그니처가 겹치거나 자모 한 자리만 다를 확률이 매우 낮기 때문이다.

→ 정공법으로 **파트너 확장**을 추가했다. seed 단어의 초성·종성 자음을 **같은 입모양 그룹의
다른 자음**으로 치환해(밥→밤·팝, 물→불·풀·뿔) 최소대립/동구형이음 후보를 규칙으로 만들고,
Claude에게 **"이 중 실제 존재하는 표준 한국어 단어만"**을 물어 실재성만 판정시킨다(LLM 환각
방지: 후보에 있던 글자만 채택). 중성(모음)은 입모양이 도드라져 동구형이음이 아니므로 건드리지
않는다. 이로써 "생성은 LLM, **관계는 규칙**" 원칙을 지키면서 쌍을 확실히 확보한다.
- 신규 함수: `content_rules.compose_syllable`(자모→음절), `content_rules.lookalike_candidates`(파트너 후보),
  `content_pipeline.filter_real_words`(실재성 배치·병렬 판정).
- 병렬화: 단어 생성·실재 판정·문맥 문항을 `asyncio.gather`로 동시 호출(초기 순차 구현은 타임아웃).

### G.5 저장·로더·CLI와 end-to-end 검증
- **저장**: 생성물은 `backend/data/curriculum/candidates_<시각>.json`(status="candidate", 검수 대기).
- **승인·병합 로더**: `curriculum.py`가 기동 시 `data/curriculum/approved.json`을 읽어 기존
  상수(WORD_BANK·MINIMAL_PAIRS·CLOSURE_ITEMS)에 중복 없이 병합하고 파생 인덱스를 재계산한다.
  파일이 없으면 아무 것도 하지 않아 기존 동작이 유지된다(**비파괴**). 생성물은 `.gitignore` 대상.
- **CLI**: `scripts/gen_content.py --words N --closures M [--auto-approve]`.

**검증(로컬 실행).**
- 파이프라인 1회(seed=기존 단어): **22초**에 단어 96·쌍 359·문항 6. 쌍이 수동 10개에서 359개로.
- CLI 1회(words 40, auto-approve): 단어 134·쌍 516·문항 8, `approved.json` 생성.
- 로더 병합 확인: `import curriculum` 시 WORD_BANK **24→154**, MINIMAL_PAIRS **10→520**,
  CLOSURE_ITEMS **5→13**. `word_partners('손')`이 병합분을 정상 반영(파생 인덱스 재계산 확인).
- 문맥 문항 품질 예: "파도가 치면 ___에 올라와야 해"(뭍/붓), "이 김치찌개 ___이 싱겁지 않아?"(맛/팔).
- **정직한 한계**: 실재 필터가 완벽하지 않아 candidate에 희귀어·비표준어(무름·파토 등)가 섞인다.
  이는 계획서가 규정한 **'규칙 검사 + 사람 검수' 이중 게이트의 사람 검수 단계**에서 거른다.
  `--auto-approve`는 개발·시연용이며 실제 배포 콘텐츠는 검수를 거친다.

**변경 파일(G.3~G.5).** `backend/content_rules.py`(compose·파트너 후보 추가),
`backend/content_pipeline.py`(실재 필터·파트너 확장·병렬화), `backend/curriculum.py`(승인 병합 로더),
`scripts/gen_content.py`(신규 CLI), `.gitignore`(생성물 제외).

### G.6 콘텐츠 품질 강화 — 빈도 게이트·품사 필터·난이도 tier
**문제.** 실재 필터가 LLM 판정에만 의존해 candidate에 희귀어·비표준어가 섞였다
(무름·파토 같은 비단어, 룬·팜 같은 외래어, 솟·쏜 같은 동사 활용형).

**해결(3중 게이트).**
1. **빈도 게이트(결정론적).** `wordfreq`의 한국어 빈도 사전으로 명백한 비단어를 컷한다.
   한국어 `wordfreq`는 원래 MeCab(형태소 분석기)을 요구하지만, `get_frequency_dict('ko')`로
   **토큰 사전을 직접 조회하면 MeCab 없이** 동작함을 확인해 그 경로를 썼다. 미등재어는
   빈도 0이라 무름·파토·농무·바랑 등이 결정론적으로 걸린다. 생성 단어는 기초 어휘여야
   하므로 zipf≥3.0, 파트너 후보는 실단어 보존을 위해 zipf≥2.5로 1차 컷한다.
2. **LLM 품사 필터 강화.** 빈도를 통과한 회색지대(룬 4.31·마다 5.3처럼 빈도는 높으나
   부적합한 것)를 LLM이 정밀 판정한다. 프롬프트에 제외 대상(동사·형용사 어간·활용형,
   조사·어미·수사·관형사, 고유명사, 외래어)과 예시('솟'=솟다 어간, '룬'=외래어)를 명시했다.
3. **빈도 기반 난이도 tier(계층화).** 빈도로 tier를 자동 부여한다(zipf≥4.5→1, ≥3.5→2, 그 외 3).
   빈도로는 난이도를 완전히 가를 수 없지만(토기 3.27 살려야 vs 보도 4.89 빼야 — 겹침),
   어려운 실단어(토기·노기·도기·톳)를 tier 3으로 태깅해 **초급자에게 노출되지 않게**
   격리한다. 이는 계획서 G의 '난이도 개인화' 토대이기도 하다.

**검증.** 개선 후 재생성에서 활용형·비단어가 사라짐(솟·쏜·룬·무름 등 제거 확인). tier 분포도
의도대로(사과·물·밥 = t1, 도끼·연필·김치 = t2, 토기·노기·도기·톳 = t3). 회귀 테스트에
빈도·tier 케이스를 더해 6개로 확대(wordfreq 미설치 환경에선 자동 스킵).

**정직한 한계.** '실재하지만 어려운 한자어'(포로·노기)는 실재성 판정을 통과하며, 빈도로도
완전히 걸러지지 않는다. tier 격리로 영향을 줄이되, 최종 품질은 계획서가 규정한 **사람 검수**로 확보한다.

**의존성.** `wordfreq`는 **콘텐츠 생성 전용**이라 `backend/requirements-gen.txt`로 분리했다.
`content_rules.py`는 optional import 하며, 없으면(`HAS_FREQUENCY=False`) 빈도 게이트를 건너뛰고
LLM 판정만 한다 → **앱 런타임(배포)에는 영향이 없다.**

**변경 파일(G.6).** `backend/content_rules.py`(빈도·tier), `backend/content_pipeline.py`(빈도 게이트·품사 필터),
`backend/test_content_rules.py`(빈도·tier 테스트), `backend/requirements-gen.txt`(신규).

### G.7 지식추적 개인화 · 사람 검수 도구 · 프론트 연동 (G 완주)
계획서 §3.7 후반부("학습 기록으로 음소별 숙달을 추정하는 지식추적으로 다음 문항을 개인화")와
'규칙 검사 + 사람 검수' 이중 게이트의 나머지 절반을 구현해 축 G를 완주했다.

**① 지식추적(`backend/knowledge_tracing.py` 신규).** 기존 `WeakViseme`(viseme별 오답·시도)를
토대로, 정통 BKT(Bayesian Knowledge Tracing)의 경량판으로 음소별 숙달도를 추정한다.
- 숙달도 = 베타-이항 사후평균 `(성공+1)/(시도+2)` — 시도가 적으면 0.5 근처의 불확실 값으로 수렴.
- 최근 오답 감쇠(반감기 7일) — 최근에 틀린 음소일수록 낮게 본다.
- `weakest_visemes`로 표적 음소, `overall_level`로 난이도(1~5)를 정한다. 순수 함수(now 주입)라 결정론적.

**② 개인화 선별(`content_rules.select_personalized`).** 표적 음소를 많이 포함하고 tier가 난이도
이하인 단어·최소대립쌍·문맥 문항을 앞세운다(필터가 아니라 정렬 → 콘텐츠가 적어도 항상 응답).

**③ API(`GET /api/curriculum/next`).** WeakViseme 로드 → 지식추적 → 개인화 콘텐츠 + 표적 음소
레슨(이름·설명·대표음절·숙달도)을 반환. 기존 `recommended-level`(문장 난이도만)을 대체하지 않고 보완한다.

**④ 사람 검수 도구(`scripts/review_content.py` 신규).** candidates를 훑어 승인분만 approved.json으로
승격한다. 대화형(항목별 y/N/q)과 tier 기반 일괄 승인(`--accept-tier N`)을 지원한다. 이로써
`--auto-approve`(개발용)와 별개로 계획서가 요구한 **사람 검수 경로**가 생겼다.

**⑤ 프론트 연동.** `api.js`에 `curriculumAPI.getNext`, `Dashboard.jsx`에 `NextUpCard`(대시보드 최상단
개인화 카드: 약점 음소 칩 + 숙달도 % + 추천 단어 + 연습 이동 버튼). 무채색 기반에 약점만 rose로
강조하는 위계형 레이아웃.

**검증.** 지식추적 테스트 4개 통과(숙달도 순서·최근성 감쇠·표적·미학습 유저). 개인화 selector가 표적
음소(양순음·치경음) 포함 단어를 앞세움을 확인. 검수 CLI 실측: 후보 134단어 중 tier≤1인 40개만
승인되고 로더가 병합(WORD_BANK 24→60). 백엔드 문법·라우트 정상. 회귀 테스트(규칙 6·엔진 11) 유지.
프론트 빌드 검증은 로컬에서 수행(iCloud 경로는 node_modules eviction으로 빌드 불가).

**변경 파일(G.7).** `backend/knowledge_tracing.py`(신규), `backend/test_knowledge_tracing.py`(신규),
`backend/content_rules.py`(select_personalized), `backend/main.py`(`/api/curriculum/next`),
`scripts/review_content.py`(신규), `frontend/src/api.js`, `frontend/src/pages/Dashboard.jsx`.

> **G 완주 요약.** 대량화 엔진(생성·규칙 게이트·저장·로더) → 품질(빈도·품사·tier) →
> 개인화(지식추적) → 사람 검수 도구 → 프론트 연동까지, 계획서 §3.7 G의 전 범위를 구현했다.
> 실제 배포 콘텐츠는 검수를 거쳐 approved.json에 반영한다.

## J. 안 보이는 소리의 시각 증강 (Cue Overlay)

### J.0 배경/목표
계획서 §3.10: 동구형이음(입모양이 같아 눈으로 구별 안 되는 음소)은 유성·기식·비음처럼
입술 밖으로 드러나지 않는 자질에서 갈린다. 입모양이 같은 순간에만 그 자질에 대응하는
**최소 시각 기호**를 겹쳐 구분을 돕고, 숙달도가 오르면 기호를 소거(페이딩)한다. 손 대신
화면 기호로 자동 생성·소거하는 큐드 스피치(Cornett 1967)의 재해석이다. 규칙 기반이라
한국어 조음 데이터 없이 착수한다.

### J.1 자질↔기호 매핑과 오버레이 계산 (`backend/cue_overlay.py` 신규)
기존 엔진 자산을 그대로 썼다. `engine`의 음소 분류(평/경/격/비음/유음)가 이미 동구형이음을
가르는 자질을 담고 있어, 별도 음향 분석 없이 자질을 뽑는다.
- 자질↔기호: 격음(ㅋㅌㅍㅊㅎ)→기식(바람), 경음(ㄲㄸㅃㅆㅉ)→긴장(힘), 비음(ㄴㅁㅇ)→울림(코).
  **평음은 기준이라 기호가 없다** — 무리 안에서 '기호 없음 vs 바람 vs 긴장 vs 울림'으로 갈린다.
- `generate_cues(text)`: '소리 나는 대로' 변환한 음절 타임라인에서, 동구형이음 무리(양순 1 +
  입 안쪽 6·7·8·10)에 속하는 자음에만 기호를 얹을 지점을 계산한다.
- **개인화 소거**: `mastery`(지식추적 G)를 주면 숙달도 높은 음소의 기호를 소거(페이딩),
  `target_visemes`를 주면 표적 음소만 남긴다(집중 학습). 스캐폴딩·페이딩 학습 원리.

### J.2 API·프론트
- **API `GET /api/cues`**: 문장의 기호 지점과 범례를 반환. 로그인 유저는 WeakViseme→지식추적
  숙달도로 **자동 페이딩**한다.
- **프론트** `components/CueBadges.jsx`(신규): 음절 위에 자질 기호를 SVG 배지(≈ 기식 / ◆ 긴장 /
  ∿ 울림)로 얹는다. 자질별 3색(sky·amber·violet) 의미 팔레트. `VisemeLiteracy`의 예시 단어에
  연동해 "같은 입모양, 다른 기호로 구분"을 보여준다.

### J.3 검증
규칙 테스트 4개 통과(격/경/비 기호·평음 기준·페이딩·표적). 실측: 밥=기호 없음(전부 평음),
팔=ㅍ 기식, 빵=ㅃ 긴장+ㅇ 울림, 나무=ㄴㅁ 울림. 페이딩(양순 숙달 시 ㅃ 소거)·표적(치경만) 확인.
백엔드 문법·라우트 정상, 프론트 빌드 통과.

**변경 파일(J).** `backend/cue_overlay.py`(신규), `backend/test_cue_overlay.py`(신규),
`backend/main.py`(`/api/cues`), `frontend/src/api.js`, `frontend/src/components/CueBadges.jsx`(신규),
`frontend/src/pages/VisemeLiteracy.jsx`.

## D. 웹캠 입모양 실시간 채점 (Webcam Mouth Check)

### D.0 배경/목표
계획서 §3.4: 현행은 아바타로 올바른 입모양을 보여줄 뿐, 학습자가 실제로 맞게 만드는지는
확인하지 못한다. 즉 '보고 따라 하고 교정하는' 학습 루프가 닫히지 않았다. 웹캠으로 학습자의
입모양을 실시간 채점해 이 루프를 닫는다. **영상은 서버로 보내지 않고 기기 안에서만 처리**한다.

### D.1 채점 로직(`frontend/src/lib/mouthScore.js` 신규, 순수 함수)
MediaPipe Face Landmarker가 브라우저에서 뽑는 얼굴 blendshape(ARKit 계열 52계수) 중 입 관련
차원만 골라, 목표 비심의 기준 프로파일과 **코사인 유사도**로 채점한다.
- `VISEME_PROFILES`: viseme(1~10)별 목표 blendshape(조음 음성학 근거의 규칙값). 예: 양순(1)=
  mouthClose↑·jawOpen↓, 개방모음(2)=jawOpen↑, 원순(4)=mouthPucker·mouthFunnel↑, 전설(3)=mouthStretch↑.
- `scorePercent`(0~100)와 `coachHint`(가장 부족한 차원을 "입을 더 벌려" 식으로 안내).
- **검증(node)**: 목표를 그대로 넣으면 자기 매칭 100, 교차는 낮음(viseme1↔2 = 6점 등). 코칭도
  정확(입 다문 채 개방모음 → "입을 더 벌려", 입술 벌린 채 양순 → "입술을 더 붙여").

### D.2 웹캠 컴포넌트(`frontend/src/components/WebcamMouthCheck.jsx` 신규)
`@mediapipe/tasks-vision`의 FaceLandmarker(GPU delegate, VIDEO 모드, blendshape 출력)를 로드해
`getUserMedia` 웹캠 프레임을 `requestAnimationFrame`으로 실시간 채점한다. 점수·코칭·프라이버시
문구("영상은 기기 안에서만 처리·저장/전송 안 함")를 표시하고, 언마운트 시 스트림·모델을 정리한다.
- **경량화**: MediaPipe 번들이 커서 `React.lazy` + '펼치기' 토글로 **펼칠 때만 로드**한다.
  빌드에서 `WebcamMouthCheck` 청크(149KB gzip 46KB)가 초기 번들과 분리됨을 확인.
- `VisemeLiteracy`(입모양 학습)의 아바타·설명 아래에 붙여 "아바타로 배우고 → 웹캠으로 따라하기"
  흐름을 만든다. 목표 viseme은 현재 학습 중인 그룹으로 넘긴다.

### D.3 검증·한계
채점 로직 node 검증 통과, 프론트 빌드 통과(청크 분리 확인), `package.json`에 의존성 반영.
**정직한 한계**: (1) 기준 프로파일은 규칙 근사값으로, 실제 정합은 웹캠 실측 보정을 전제한 초기값이다.
(2) 계획서의 '자체 립리딩 모델'(단어 단위 인식)은 데이터·학습이 필요해 이번 범위 밖으로 두고
웹캠 채점만 구현했다. (3) 실제 카메라 동작 확인은 dev 환경(카메라 권한)에서 수행한다.

**변경 파일(D).** `frontend/src/lib/mouthScore.js`(신규), `frontend/src/components/WebcamMouthCheck.jsx`(신규),
`frontend/src/pages/VisemeLiteracy.jsx`, `frontend/package.json`(@mediapipe/tasks-vision).

> **Phase 0 완료.** G(콘텐츠 대량화) · J(시각 증강) · D(웹캠 채점)로 데이터·인프라 의존이 낮은
> 로컬 착수 축들을 모두 구현했다. 다음 단계는 Phase 1(B 전사 비의존 D-GOP · C 지각공간)이다.

### Phase 0 통합 — 축 간 연결 (통합 학습 루프)
각 축이 따로 돌지 않도록 서로 이어, '연습 → 기록 → 약점 파악 → 개인화·기호 소거'의 루프를 닫았다.
- **D → 지식추적(G)**: 웹캠 채점 결과를 `POST /api/curriculum/mouth-attempt`로 WeakViseme에 누적.
  '익힘 기록' 버튼으로 최고점을 남기면, 지식추적이 그 기록을 읽어 개인화·시각 증강 페이딩의 근거로 쓴다.
- **J → 학습 화면**: 시각 증강 오버레이(CueBadges)를 단어 학습(`WordStage` 정답)·문장 연습
  (`Practice` 정답)에 연동. 정답 공개 시 그 단어·문장의 '안 보이는 자질'이 기호로 드러난다.
- **G tier → 프론트**: `WordStage` 정답에 난이도 tier 배지를 노출.

**변경 파일(통합).** `backend/main.py`(mouth-attempt), `frontend/src/api.js`(recordMouth),
`frontend/src/components/WebcamMouthCheck.jsx`(기록 버튼), `frontend/src/pages/WordStage.jsx`(J·G),
`frontend/src/pages/Practice.jsx`(J). 백엔드 문법·프론트 빌드 검증 통과.

### D.4 개인 캘리브레이션(기준 프로파일 실측 보정) · 립리딩 데이터 조사
**개인 캘리브레이션.** 규칙 근사값의 오차를 줄이려 '본뜨기'를 추가했다. 사용자가 각 viseme(대표
음절 마·아·이·우…)를 직접 지으면 그 순간 blendshape를 1.5초 모아 평균내 **개인 기준 프로파일**로
localStorage에 저장하고, 이후 채점은 개인 실측값을 쓴다(없으면 규칙값 폴백). 영상·계수는 기기 밖으로
나가지 않는다. `MouthCalibration.jsx`(신규, 10단계 본뜨기 UI), `mouthScore.js`(profiles 파라미터·
`averageBlendshapes`·localStorage 저장/로드), `WebcamMouthCheck.jsx`(본뜨기 진입·개인 프로파일 채점). 빌드 통과.

**자체 립리딩 모델 — 데이터 조사.** 데이터·GPU가 필요한 립리딩 모델은 이번엔 착수 대신 사전 조사만
수행해 `docs/lipreading-data-research.md`에 정리했다. 요지: **OLKAVS(= AI Hub 「립리딩 음성인식 데이터」,
문장 단위·무료·영상 수 TB)가 유일한 대규모 한국어 코퍼스**이고, word-level 코퍼스는 없어 목표를
**closed-set 단어 분류로 축소**하는 것이 현실적이다. 사전학습은 한국어로 학습된 OLKAVS V-model을
초기가중치로 우선, AV-HuBERT는 비상업 라이선스·진입장벽으로 후순위. 이 축은 Phase 2 백본(A)과 함께 추진한다.

**변경 파일(D.4).** `frontend/src/lib/mouthScore.js`, `frontend/src/components/MouthCalibration.jsx`(신규),
`frontend/src/components/WebcamMouthCheck.jsx`, `docs/lipreading-data-research.md`(신규).

---

# 제2기 고도화 — Phase 1

## B. 전사 비의존 발음정확도(D-GOP)

### B.1 핵심 로직 (`backend/dgop.py` 신규, 순수 함수)
계획서 §3.2: 전사 없이 목표 음소가 놓일 구간의 음향이 그 음소에 얼마나 부합하는지를 사후확률로
점수화한다(GOP). 목표 문장을 이미 알기 때문에 전사가 필요 없다. 다만 표준 GOP는 정상 발화를
전제해 **뭉갠 농인 발화에서 오히려 과신**(점수 붕괴적 상승)한다. D-GOP는 예측 분포의 불확실성
(엔트로피·상위 확률 여유)으로 naive 점수를 보정하고, 음향이 불확실한 구간일수록 영상(입모양)에
가중해 후기 융합한다.
- `normalized_entropy`·`top_margin`·`phone_confidence`: 예측 분포의 불확실성 정량화.
- `dgop_phone`: naive(목표 사후확률)를 그 구간 신뢰도로 보정 → 분포가 평평할수록 과신을 막는다.
- `fuse_audio_visual`: 음향 불확실할수록 영상(웹캠 D축) 가중치↑ 후기 융합(농인은 음성이 불안정한
  반면 입모양은 상대적으로 안정적).
- 음향 모델 비의존(사후확률 분포를 입력받는 순수 함수)이라 결정론적으로 테스트된다.

**검증(테스트 4개).** 과신 붕괴 보정 실증: 명료 발음 naive 0.85 → D-GOP 0.38, 뭉갠 발음
naive 0.40(과신) → **D-GOP 0.002**(불확실성 0.995로 강하게 할인). AV 융합: 음향 불확실 시
visual_weight 0.3 → 0.65로 자동 상승.

### B.2 다음 (음향 백본 — torch 필요)
실제 사후확률을 공급하는 음향 추론(wav2vec2/WavLM 강제정렬)은 `torch`·`transformers`가 필요한데
현재 로컬 venv에 미설치다. 음향 백본 어댑터 + 기존 전사 방식 폴백으로 `/api/speak/assess`를
D-GOP 경로로 교체하는 것은 torch 설치 후 2차로 진행한다. 계획서도 "전사 방식을 폴백으로 남긴다".

**변경 파일(B.1).** `backend/dgop.py`(신규), `backend/test_dgop.py`(신규).

## C. 지각 자원 — 규칙 기반 (동구형이음 사전 · 독화 난이도 지수)

### C.1 규칙 기반 공개 자원 (`backend/perceptual.py` 신규)
계획서 §3.3: 지각공간 임베딩(대조학습)은 데이터·연산이 필요하지만, 그 산출물의 상당 부분
(동구형이음 사전·독화 난이도 지수·표준 평가셋)은 **비심 규칙만으로 결정론적으로 도출**된다.
한국어 독화에는 이런 표준 자원이 거의 없으므로, 데이터 없이 먼저 만들어 앱 밖 연구·교육에도
쓸 수 있게 공개한다.
- `homophene_dictionary`: 음소를 입모양(viseme)으로 묶은 동구형이음 사전 + 가시성.
- `invisibility`: 단어의 '안 보이는 정도'(저가시성 음소 비중).
- `word_difficulty`/`sentence_difficulty`: 난이도 지수 = 안 보이는 정도(0.6) + 혼동 이웃 밀도(0.4).
  (계획서 정의: 구성 음소의 이웃 밀도와 동구형이음 비율)
- `scripts/export_perceptual.py`: `docs/perceptual-resources.json`으로 공개 export.

**검증(테스트 4개).** 난이도가 의미대로 갈림: 모음 단어(이·우유·오이) 0.0(잘 보임),
치경음 단어(달·탈·살·쌀) 0.64(안 보임 + 서로 동구형 이웃 많음). 동구형이음 사전에 양순음
ㅂ·ㅃ·ㅍ·ㅁ, 치경음 저가시성 정확. 자원 24단어·22쌍 생성.

지각공간 임베딩(데이터 기반)이 준비되면 이 규칙값을 보정한다(음향·영상 백본과 함께 Phase 2).

**변경 파일(C.1).** `backend/perceptual.py`(신규), `backend/test_perceptual.py`(신규),
`scripts/export_perceptual.py`(신규), `docs/perceptual-resources.json`(신규, 공개 자원).

### D.5 OLKAVS 전처리 파이프라인 검증
AI Hub 립리딩 샘플(OLKAVS)의 라벨 JSON(문장별 타임스탬프 + 프레임별 입술 바운딩박스)로
원본 mp4를 문장 단위 입술 ROI 클립(96x96) + 텍스트 라벨(labels.jsonl)로 잘라내는 전처리를
구현했다. torch 없이 ffmpeg만으로 동작(계획서 우선순위 1: 전처리 파이프라인 검증). 샘플에서
6개 클립 생성 확인. 실제 립리딩 학습(closed-set 단어 분류)은 Phase 2(GPU)에서 이 클립을 입력으로.
**변경 파일(D.5).** `scripts/preprocess_olkavs.py`(신규).

## I. 디지털 독화 표준검사 (Placement Test)
계획서 §3.9. 독화 실력을 재는 표준 검사가 없어 수준·향상도 판단 근거가 없다. 지각 난이도
지수(perceptual, 축 C)로 난이도를 통제한 '입모양→단어' 4지선다 배치검사를 구성한다. 오답
보기는 정답과 시각적으로 혼동되는(동구형이음·최소대립) 단어를 우선 배치해 독화 변별력을 잰다.
- `assessment.py`(신규): `build_placement_items`(난이도 스펙트럼 균등표집), `score_placement`
  (능력=통과 최고난이도, 음소별 오류 프로파일, 시작단계 추천). perceptual·content_rules 재사용.
- API: `GET /api/assessment/placement`, `POST /api/assessment/score`.
검증(테스트 4): 난이도 오름 문항, 혼동보기(도마↔소파·노파, 컵↔팥·겁), 채점·수준·시작단계 추천.
프론트 검사 페이지는 후속. 지각공간 임베딩 준비되면 난이도 통제 정교화(Phase 2).
**변경 파일(I).** `backend/assessment.py`(신규), `backend/test_assessment.py`(신규), `backend/main.py`.

## K. 입술 너머 확장 조음 단서 (Face Cues)
계획서 §3.11. 웹캠 채점(D)은 입술 중심이지만, 말은 얼굴 전체에 조음 흔적을 남긴다(턱·볼·코).
이 신호를 뽑아 입 밖으로 잘 안 드러나는 자질(비음·압력)의 상관 신호를 보조적으로 추정한다.
- `frontend/src/lib/faceCues.js`(신규, 순수): MediaPipe 입술 외 blendshape(jawOpen·cheekPuff·
  noseSneer·mouthPress)에서 jaw_open·nasal·cheek_pressure 신호 추정. node 검증(턱0.7·코울림0.6·볼압력0.75).
- `WebcamMouthCheck`에 '입술 너머 신호(보조·실험)' 미니 바 3개 표시.
**정직한 한계**: 미세 신호는 조명·개인차에 민감하므로 판정이 아니라 보조 단서로만 쓴다(계획서 명시).
J 기호 뒷받침·B 융합 입력 후보(음향·영상 백본과 함께 Phase 2에서 정교화).
**변경 파일(K).** `frontend/src/lib/faceCues.js`(신규), `frontend/src/components/WebcamMouthCheck.jsx`.
