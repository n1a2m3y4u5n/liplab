# LIPLAB UI 변경 내역 (Figma 기준)

> `LIPLAB_UI_HANDOFF.md`(화면 ↔ 코드 연결 명세)와 함께 읽는다. 이 문서는 명세서 작성 **이후에 Figma에서 바뀐 내용**과 **구현에서 자주 어긋나는 지점**을 정리한 것이다.
> 충돌 시 우선순위: 이 문서 > `LIPLAB_UI_HANDOFF.md` > `LIPLAB_REDESIGN_SPEC.md`(폐기 예정).

---

## 0. 지금 반드시 고쳐야 할 구현 오류

### 0-1. 목록의 화살표는 버튼이 아니라 "스크롤 안내 표시"다

**현재 구현:** 목록 아래 화살표가 버튼이고, 누르면 목록이 펼쳐진다.
**바른 동작:** 목록 자체가 세로 스크롤되고, 화살표는 "아래에 더 있다"는 것을 알리는 **장식 표시**다. 클릭·포커스 대상이 아니다.

| Figma 노드 | 위치 |
|---|---|
| `Scroll hint` (그라데이션 페이드) + 그 안의 `More`(원형 화살표) | 복습 탭 `Card / 복습 항목`, 복습 선택 모드, 모바일 복습, 회차 상세 모달 `Item list` |

규칙
1. 목록 컨테이너에 **최대 높이 + `overflow-y: auto`**를 준다. 항목은 잘리지 않고 스크롤로 계속 이어진다.
2. `Scroll hint`는 목록 하단에 겹치는 **흰색 그라데이션 페이드**다. 목록 흐름을 밀어내지 않도록 `position: absolute; bottom: 0`로 겹쳐 놓는다.
3. 화살표 원은 **버튼이 아니다.** `<button>`이나 `onClick`을 쓰지 않는다. `pointer-events: none`, `aria-hidden="true"`로 둔다.
4. 스크롤이 **맨 아래에 닿으면 페이드와 화살표를 숨긴다**(`opacity: 0`, 150~200ms 전환). 스크롤 위치로만 판단한다.
5. 목록이 컨테이너보다 짧아 스크롤이 필요 없으면 **처음부터 표시하지 않는다.**
6. "더 보기", "N개 더 있어요" 같은 **문구를 넣지 않는다.** Figma에도 없다.

```jsx
// 참고 구조
<div className="relative">
  <ul ref={listRef} onScroll={handleScroll} className="max-h-[420px] overflow-y-auto">
    {items.map(...)}
  </ul>
  {canScrollMore && (
    <div aria-hidden className="pointer-events-none absolute inset-x-0 bottom-0 h-13
                                bg-gradient-to-t from-white to-transparent
                                flex items-end justify-center pb-2">
      <ChevronDownCircle />
    </div>
  )}
</div>
```

### 0-2. 한글 받침 잘림

버튼·칩 안의 글자에 줄높이를 1로 주면 "명", "요" 같은 글자의 받침 아래가 깎인다. **버튼 라벨은 `line-height: 1.5` 이상**을 준다.

### 0-3. 잘라내기(overflow) 주의

다음 요소는 자식이 틀 밖으로 조금 나가는 것이 정상이므로 `overflow: hidden`을 걸지 않는다.
- 레슨 화면 질문 줄의 **북마크 버튼**(제목 줄보다 커서 아래로 7px 넘침)
- 섹션 제목 텍스트 래퍼

반대로 **반드시 잘라야 하는 것**: 워터마크 아이콘이 걸친 카드(§3-1), 화면 축소본 프레임.

### 0-4. 3D 버튼의 선택 상태 높이

3D 버튼은 아래 테두리가 두꺼운 스타일이다. 선택 시 아래 테두리가 얇아지면서(5px → 2.5px) 버튼 높이가 2.5px 줄어 **한 칸만 내려앉아 보인다.** 선택 상태에 위 패딩을 더해 높이를 맞추거나, 버튼 묶음에 `align-items: stretch`를 준다.

---

## 1. 새로 생긴 화면

| 화면 | Figma 섹션 | 비고 |
|---|---|---|
| 에러 — 404 (데스크톱·모바일) | 10. 로딩 · 에러 | 눈 X자 DOKA + "페이지를 찾을 수 없어요" + `404` + 학습으로 돌아가기 |
| 에러 — 문제 발생 (데스크톱·모바일) | 10. 로딩 · 에러 | 다시 시도(주 버튼) · 학습으로 돌아가기 |
| 상황별 시나리오 — 문장 테스트 선택 상태 | 11. 연습 기능 | AI 대화 선택 상태와 한 쌍 |
| 복습 탭 — 선택 모드 | 06. 과제 · 복습 상세 | 지우기 → 체크박스 + "선택한 N개 지우기" |
| 배지 상세 모달 | 06. 과제 · 복습 상세 | 배경 블러, 카드 없음 |
| 사용법 가이드 — 개발자 소개 탭 | 09. 사용법 가이드 | 팀원 5명 + 저장소 링크 |

## 2. 삭제된 기능·요소

| 삭제 | 조치 |
|---|---|
| **다자 대화** (연습 기능) | 연습 탭 카드·연습 기능 화면·가이드 설명에서 모두 제거. 진입점을 없애고, 라우트·파일(`/learn/conversation-multi`, `MultiConversation.jsx`) 삭제 여부는 별도 결정 |
| 독화 레슨 **"다시 보기"** 버튼 | 입모양 영상은 무한 반복이므로 버튼 없음 |
| 발화 레슨 **"들어보기"** 버튼 | 청각장애 대상 서비스이므로 소리 재생 버튼을 두지 않음. 모범 발음은 입모양 아바타로 제공 |
| 자가진단 **확인 모달** | 프로필 → "자가진단 다시 하기"는 모달 없이 바로 `/learn/placement` |
| 사용법 가이드 **시작하기 탭**, **알아두면 좋아요 탭** | 전자는 삭제, 후자는 개발자 소개로 대체 |
| 학습 경로 옆 **큰 마스코트** | 경로 노드가 모두 DOKA이므로 중복 제거 |
| 프로필 **로그아웃 버튼** | 계정 설정 모달 안으로 이동 |

---

## 3. 공통 규칙 추가분

### 3-1. 워터마크 카드
숫자·요약 카드는 아이콘을 카드 오른쪽 위에 크게 걸쳐 **잘리게** 둔다.
- 회전 `+20°`(반시계), 투명도 20% 내외(채움 아이콘은 17%), 카드 `overflow: hidden`
- 아이콘 위치는 **모양의 시각적 중심**을 기준으로 맞춘다(프레임 기준으로 맞추면 별·불꽃처럼 비대칭인 모양이 어긋난다)
- 별·불꽃처럼 폭이 좁은 모양은 1.2~1.4배 키워 시각적 무게를 맞춘다
- 적용: 분석 탭 스탯 3칸, 레슨 완료 스탯 3칸, 복습 히어로 카드 2개, 과제 주간 도전 카드(DOKA)

### 3-2. 학습 경로 노드 = DOKA
| 상태 | 표현 |
|---|---|
| 완료 | 트랙 색 DOKA, **투명도 55%**, 오른쪽 아래 체크 배지 |
| 현재 | 트랙 색 DOKA, **1.15배**, 바깥 링 |
| 잠김 | 회색 + **눈 감은(잠든)** DOKA |
| 건너뛰기 가능 | 연한 트랙 색 + **눈 뜬** DOKA |

### 3-3. 북마크 버튼
디자인 시스템 컴포넌트 `Bookmark button`(기본/눌림 2가지). 레슨 화면 질문 제목 오른쪽 끝에 배치. 기본은 테두리만, 누르면 아이콘이 채워진다.

### 3-4. 모달
딤 + 가운데 카드 + 오른쪽 위 원형 X. **배지 상세 모달만** 카드 없이 배경 블러 위에 요소를 띄운다.

---

## 4. 화면별 변경 내역

### 4-1. 학습 탭
- 경로 노드를 원형 버튼 → **DOKA 노드**로 교체(§3-2). 경로 옆 마스코트 제거.

### 4-2. 독화 레슨
- "다시 보기" 제거, 입모양은 영상 중앙 정렬.
- 질문 제목 오른쪽에 북마크 버튼.
- **레슨 완료 화면**: DOKA + "레슨 완료!" + 워터마크 스탯 3칸(정답률 · 획득 XP · 걸린 시간) + 다음 레슨으로 / 커리큘럼으로 돌아가기.

### 4-3. 발화 레슨
- "들어보기" 제거, 목표 단어는 카드 **가운데 정렬**.
- 상세 분석 모달: 왼쪽 "이렇게 들렸어요" 정사각형, 오른쪽 음소별 정확도 + 융합 점수 2단. 코칭 문구는 **"DOKA의 한마디"** — 분홍 DOKA + 구분선 + 문구.

### 4-4. 연습 탭 · 연습 기능
- 카드 4개 + 공사 중 카드(다자 대화 제거).
- **모든 연습 기능 화면 오른쪽 위에 나가기 X 버튼**(40px 원, 제목 줄 오른쪽 끝). 누르면 연습 탭 목록으로.
- **자유 발화**: 입력 + 입모양 + 성도 단면을 **카드 하나**에 넣고 구분선으로 구역을 나눈다. 입모양·성도 단면은 좌우 2단, "지금 발음 중 (문장)"은 두 그림 **아래 가운데**.
- **상황별 시나리오**: §5 참고(전면 개편).

### 4-5. 복습 탭
- 히어로 카드 2분할: **오답(보라)** · **북마크(파랑)**, 각 카드에 워터마크 아이콘(⊗ / 책갈피).
- 목록: 필터 칩(전체 · 오답 · 북마크) + 오른쪽 **지우기** 버튼 → 선택 모드.
- 항목 간격은 구분선 위아래 **동일**(항목 묶음 간격 0 + 각 항목 위아래 패딩 16px).
- 우측 패널 복습 카드: `오답 N개 │ 북마크 N개`(가운데 정렬, 숫자만 크게, 오답 보라·북마크 파랑) + 복습하기 버튼.

### 4-6. 분석 탭
- 스탯 3칸을 **워터마크 카드**로.
- **학습시간 추이**(보라 막대, 1h 40m 형식) + **정확도 추이**(초록 선 + 값 표시) 2개 그래프.
- 하단 버튼 3개: 활동 캘린더 · 회차 히스토리 · **전체 통계**(이전 "전체 분석").
- 회차 히스토리: 점수 → **정답률 %**, 오늘 기록은 시각 대신 **"오늘"**, 행마다 정답률 막대.
- 회차 상세(발화): 문제별 음소 칩 + **들린 발음(transcript)** + 점수. 모달 폭 640.

### 4-7. 과제 탭
- 주간 도전 카드의 트로피 → **워터마크 DOKA**(씩 웃는 입).
- 배지 클릭 → **배지 상세 모달**(배경 블러, 은은한 흰 원, "상위 N%가 획득했어요", 닫기).

### 4-8. 프로필 탭
- 메뉴 4개: 사용법 가이드(모달) · 자가진단 다시 하기(바로 이동) · 계정 설정(모달) · **학습 초기화**(모달).
- 로그아웃 버튼 제거 → 계정 설정 모달 안.

### 4-9. 온보딩 · 자가진단
- 자기 보고식 설문 → **기존 배치검사 유지**. 화면만 교체(안내 → 문항 → 결과).
- 문항 화면은 독화 레슨 템플릿 재사용. 북마크 없음, 정답 즉시 공개 없음, 하단 "정답은 끝나면 결과로 알려드려요" + **다음** 버튼.
- 결과 화면 버튼: "학습하러 가기" · **"다시 진단하기"**.

### 4-10. 사용법 가이드 모달
- 탭 11개(화면 둘러보기 · 학습 탭 · 독화 레슨 · 발화 레슨 · 독화 요령 · 연습 · 과제 · 복습 · 분석 · 프로필 · **개발자 소개**).
- 모달 높이 **680px 고정**. 왼쪽 탭 목록 + 오른쪽 내용.
- 탭 설명 문구·화면 위 번호 제거. 설명 목록은 **점(•) + 제목 + 한 줄**.
- 화면 축소본은 Figma에서 PNG로 내보내 **이미지**로 넣는다(실제 컴포넌트 재사용 아님).
- 독화 요령 탭은 카드 격자가 아니라 **큰 번호 + 구분선 + 작은 예시** 형식.

---

## 5. 상황별 시나리오 — 전면 개편

`ScenarioHub`를 **하나의 설정 폼 + 단일 시작 버튼**으로 바꾼다. 카드 3개가 아니라 **테두리 하나** 안에 구분선으로 구역을 나눈다.

```
┌─ 시나리오 설정 ─────────────────────────┐
│ 어떤 상황인가요?   [ 직접 입력 ]            │
│ ──────────────────────────────────  │
│ 난이도            1단계 · 아주 쉬움         │
│ ●──○──○──○──○   (1~5 슬라이더)        │
│ 짧고 쉬운 문장            길고 빠른 대화      │
│ ──────────────────────────────────  │
│ 어떻게 연습할까요?                        │
│ [문장 테스트] ○     [AI 대화] ✓           │
│   └ AI 대화일 때만 ↓                     │
│      대화 상대  [1:1 대화][여러 명 대화]     │
│      인원      [2명][3명][4명]           │
└────────────────────────────────────┘
[              시작하기              ]
```

1. **상황 프리셋 칩(카페·병원 등)을 없앤다.** 텍스트 입력만 둔다. 플레이스홀더 `예: 카페에서 음료 주문하기`.
2. **난이도는 슬라이더**다. `min=1 max=5 step=1`로 **정수에만 멈추게** 한다. 눈금 5개와 숫자를 표시하고, 지나온 눈금은 트랙 색으로 채운다. 제목 줄 오른쪽에 `N단계 · 라벨`(1 아주 쉬움 / 2 쉬움 / 3 보통 / 4 어려움 / 5 아주 어려움), 아래 양 끝에 "짧고 쉬운 문장" ↔ "길고 빠른 대화".
3. **연습 방식은 문장 테스트 / AI 대화 2개**이고, 선택 표시는 오른쪽 위 체크 동그라미.
4. **AI 대화를 고른 경우에만** 대화 상대(1:1 / 여러 명)와 인원(2~4명)이 펼쳐진다. 문장 테스트를 고르면 이 영역은 렌더링하지 않는다.
5. 카드 안에는 시작 버튼을 넣지 않는다. **카드 아래 전체 폭 버튼 하나**로 시작한다. 상황 입력이 비어 있으면 비활성.
6. 문구: "여러 명이면 누가 말하는지도 함께 찾아야 해요", "나를 포함하지 않은 상대 수예요".

> 기능 메모: AI 대화에 **여러 명 대화**가 추가되면서 기존 다자 대화 기능을 대체한다(§2).

---

## 6. 구현 체크리스트

- [ ] 목록 화살표가 버튼이 아니라 스크롤 표시로 동작하는가 (§0-1)
- [ ] 버튼 라벨 줄높이 1.5 이상, 받침이 잘리지 않는가 (§0-2)
- [ ] 북마크 버튼·섹션 제목이 잘리지 않는가 (§0-3)
- [ ] 3D 버튼 선택 시 높이가 흔들리지 않는가 (§0-4)
- [ ] 워터마크 아이콘이 카드 밖으로 잘려 나가는가, 세 카드의 중심이 같은가 (§3-1)
- [ ] 학습 경로 노드가 DOKA 4상태로 구분되는가 (§3-2)
- [ ] 연습 기능 화면마다 오른쪽 위 X가 있는가 (§4-4)
- [ ] 상황별 시나리오에서 문장 테스트를 고르면 대화 상대·인원이 사라지는가 (§5)
- [ ] 난이도 슬라이더가 정수에만 멈추는가 (§5)
- [ ] 다자 대화 진입점이 모두 사라졌는가 (§2)
